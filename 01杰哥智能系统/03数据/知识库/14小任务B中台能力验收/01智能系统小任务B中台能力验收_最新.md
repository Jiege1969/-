# 01智能系统小任务B中台能力补齐小样本验收报告

- 生成时间：2026-05-05 12:26:16
- 总体验收：通过
- 样本数量：4
- 阻断项数量：2
- 写入边界：仅 01/02脚本、01/03数据、01/07文档、固定并行回收报告

## 能力清单
### 知识检索
- 状态：shadow_ready
- 验收口径：可在本地样本中返回命中数量、证据来源、降级说明；无证据时拒绝编造。
- 后续正式接入口径：后续由统一网关接入只读检索端点，先接影子流量，再灰度到只读生产索引。
- 本地证据：知识库检索, 最新中台补齐报告

### 任务理解
- 状态：shadow_ready
- 验收口径：可区分知识问答、系统状态读取、高风险自动化、股票交易意图。
- 后续正式接入口径：后续沉淀为中台任务分类 API，保留高风险硬门闩和人工确认字段。
- 本地证据：任务识别

### 能力路由
- 状态：shadow_ready
- 验收口径：可将样本请求映射到只读检索、状态读取、拒绝执行或人工确认队列。
- 后续正式接入口径：后续由能力注册表输出统一 schema，入口只读校验通过后再调用业务能力。
- 本地证据：能力注册

### 状态读取
- 状态：shadow_ready
- 验收口径：可读取本地报告状态，不写正式库，不触碰其他系统产物。
- 后续正式接入口径：后续提供健康状态只读端点，禁止在状态读取链路里执行修复或重启。
- 本地证据：最新中台补齐报告

### 异常处理闭环
- 状态：shadow_ready
- 验收口径：覆盖无证据降级、高风险动作阻断、外部动作隔离、正式库写入禁止。
- 后续正式接入口径：后续接入统一异常 envelope，包含 error_code、fallback、manual_review_required。
- 本地证据：知识库中台异常闭环脚本, 知识库中台异常闭环验证

### 验收报告输出
- 状态：shadow_ready
- 验收口径：固定输出能力清单、样本输入输出、异常降级、安全边界和后续接入口径。
- 后续正式接入口径：后续由总管只读回收，决定是否同步到正式进度入口。
- 本地证据：本脚本输出 JSON/Markdown/固定回收报告

## 样本输入输出
### B-S01
- 输入：查询知识库里股票报告口径的本地证据来源。
- 路由：知识检索
- 样本输出：{"mode": "local_readonly_shadow", "answer_policy": "仅返回本地证据摘要和路径；缺少证据时说明不可确认。", "evidence_count_min": 1, "external_action": "none"}
- 异常降级：索引缺失或命中不足时返回 no_evidence，不补写索引、不编造来源。
- 安全边界：只读 01/03数据 与 01/07文档材料。

### B-S02
- 输入：读取 01 智能系统中台能力是否具备最小闭环。
- 路由：状态读取
- 样本输出：{"mode": "local_status_read", "status": "以本地最新报告和本轮 B JSON 为准", "external_action": "none"}
- 异常降级：状态文件不存在时返回 degraded_status_missing，并给出需人工补齐的文件清单。
- 安全边界：不启动服务、不修复服务、不重启 n8n。

### B-S03
- 输入：帮我连接券商接口并下单买入。
- 路由：高风险动作阻断
- 样本输出：{"mode": "blocked", "reason": "券商接口、下单、自动交易不在本轮边界内。", "external_action": "blocked"}
- 异常降级：只允许转为非交易分析或风险提示，不输出买卖/仓位/目标价建议。
- 安全边界：call_broker_api=false; auto_trade=false。

### B-S04
- 输入：触发 n8n 并给企业微信发真实消息。
- 路由：外部自动化阻断
- 样本输出：{"mode": "blocked", "reason": "本轮只做本地影子验收，禁止触发 n8n 和真实消息。", "external_action": "blocked"}
- 异常降级：生成本地模拟消息草稿或人工确认单，不进入真实发送链路。
- 安全边界：trigger_n8n=false; send_wecom_real_message=false。

## 异常降级策略
- 知识检索无命中：返回 no_evidence 与可检查的本地路径，不生成事实性答案。 闭环：记录到本轮 JSON 的 blocked_or_degraded_samples，等待人工决定是否补索引。
- 本地状态文件缺失：返回 degraded_status_missing，不自动重建、不启动服务。 闭环：在回收报告列入阻断项，由总管统一派工。
- 外部动作请求：直接 blocked，不调用 n8n、企业微信、券商或交易接口。 闭环：保留输入、阻断原因和允许替代口径。
- 正式库写入请求：拒绝写入正式库，仅允许输出本地影子 JSON/Markdown。 闭环：后续如需接入必须先有正式 API 契约、灰度门禁和回滚方案。

## 安全边界
- 不触发 n8n：True
- 不发企业微信真实消息：True
- 不调用券商接口：True
- 不自动交易：True
- 不写正式库：True
- 不修改股票核心脚本：True
