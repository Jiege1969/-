# 新系统隔离n8n数据目录说明

创建时间：2026-04-28

用途：作为 `jiege_v3_n8n` 的隔离数据目录，服务于 `D:\杰哥智能化系统` 内的新系统工作流。

安全边界：

- 不承载旧系统 `D:\杰哥智能体操作系统` 的 n8n 数据。
- 不自动导入工作流。
- 不自动启用工作流。
- 不调用 n8n API 进行隐式导入、启用、删除或触发。
- 当前保留的工作流母样本必须保持全禁用，直到人工上线闸口明确放行。
- 不触发企业微信真实发送。
- 不写正式业务库。
- 不包含券商接口或自动交易能力。
