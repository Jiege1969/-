# 低风险任务准入规则

- 生成时间：2026-05-08 19:32:20
- 当前状态：只登记候选，不自动执行。
- 默认：enabled=false，dry_run_only=true。

## 允许类别

| ID | 类别 | 准入说明 |
| --- | --- | --- |
| readonly_inspection | 只读巡检 | 只读取本地既有产物、脚本清单、日志摘要或状态文件，不访问外部系统。 |
| artifact_integrity_check | 产物完整性检查 | 检查指定 JSON/MD/日志文件是否存在、字段是否齐全、格式是否可解析。 |
| regression_acceptance | 回归验收 | 运行本地只读验收脚本或静态校验，禁止触发真实业务动作。 |
| evidence_archive | 证据归档 | 整理本地证据索引、摘要和验收记录，只写入本包数据/日志目录。 |
| status_package_refresh | 状态包刷新 | 刷新候选状态包或只读状态摘要，不改正式规则和运行中配置。 |

## 明确排除

- 真实发企微
- n8n触发
- 券商交易
- 税局登录
- 财税软件
- 正式规则生效
- 视频真实发布

## 硬边界

- 不真实发送企业微信，不触发 n8n，不接券商，不交易，不登录税局，不接财税软件。
- 不自动转正式规则，不改总管面板，不改一键接续包，不重载服务。