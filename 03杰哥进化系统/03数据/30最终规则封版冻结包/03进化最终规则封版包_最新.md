# 03进化系统最终规则封版包

- 创建日期：2026-05-05
- 任务批次：并行小任务O
- 包版本：2026-05-05-O-final-freeze
- 所属系统：03杰哥进化系统
- 定位：冻结03进化系统当前关键原则，作为后续规则沉淀、任务评审、并行收口和旧口径纠偏的最终参照包。
- 真实动作数量：0
- 外部服务调用数量：0

## 执行边界

- 仅新增03进化系统本地规则封版文件。
- 仅写入00总管并行回收固定报告。
- 不触发外部服务，不执行真实动作。
- 不调用Redis服务，不触发n8n，不调用webhook。
- 不写正式库，不调用券商接口，不下单，不自动交易。

## 最终冻结原则

| 规则ID | 规则名 | 冻结口径 | 阻断退化摘要 |
| --- | --- | --- | --- |
| FINAL-03-001 | 学逻辑不搬壳子 | 只学习任务契约、分层编排、只读验收、反退化检查等抽象逻辑；不得复制OpenClaw、Hermes或其他外部系统的原始代码、配置、提示词、凭据、workflow、镜像或可执行壳子。 | 阻断新增OpenClaw/Hermes原件、第三方系统克隆件、以复刻壳子替代规则抽象。 |
| FINAL-03-002 | 总管唯一中枢 | 00杰哥系统总管是唯一系统级中枢；03进化只负责经验提炼、规则沉淀、只读审计和反退化提示。 | 阻断03侧第二调度中枢、绕过00总管放行入口、规则评审自动变真实动作许可。 |
| FINAL-03-003 | 任务契约优先 | 所有跨系统、并行小任务和交付候选都必须先有任务契约；dry_run优先，验收方式先于进度声明。 | 阻断无契约施工、契约缺验收仍称完成、口头范围替代结构化契约。 |
| FINAL-03-004 | SQLite影子台账 | 状态追踪、候选记录、审计映射和低风险演练优先使用SQLite影子台账承接只读、dry_run、可回滚的本地证据。 | 阻断跳过影子台账直接写正式库、把影子台账当生产主库、未验收提升进度。 |
| FINAL-03-005 | Redis/n8n升级门槛 | Redis和n8n必须完成低风险、本地只读、dry_run、门禁清单和人工确认后才可讨论升级。 | 阻断Redis真实队列、n8n导入启用触发、webhook调用、用工具可用性替代升级门槛。 |
| FINAL-03-006 | 真实动作闸门 | 真实发送、正式库写入、外部API、企业微信、券商接口、下单、自动交易、生产调度一律默认关闭。 | 阻断脚本默认真实发送、验收脚本调用外部API、规则卡自动放行真实动作。 |
| FINAL-03-007 | 股票analysis-only | 股票相关能力只允许分析、复盘、规则沉淀、证据链核验和只读验收。 | 阻断自动交易、券商接口、买卖指令、投资承诺。 |
| FINAL-03-008 | 并行收口格式 | 并行小任务必须输出固定回收报告，包含任务目标、写入范围、产物路径、安全边界、验收命令、验收结果、真实动作数量和协作边界。 | 阻断只口头回收、报告缺少真实动作数量、md/json报告互相矛盾。 |
| FINAL-03-009 | 进度口径不得虚高 | 进度只能按已落地文件、已通过只读验证、已回收报告和已人工确认分层表达；未验收不得称完成。 | 阻断未验收宣称完成、演练冒充上线、百分比虚高掩盖阻断项。 |
| FINAL-03-010 | 旧口径可纠偏 | 旧规则、旧报告、旧说法与当前封版原则冲突时，允许以纠偏记录、反退化清单和新回收报告覆盖旧口径。 | 阻断为维持旧说法忽略新闸门、删除历史证据、纠偏无验收结论。 |

## 只读验证要求

- 验证方式：本地文件解析和字段一致性检查。
- 允许读取：封版包JSON/Markdown、反退化冻结清单JSON/Markdown。
- 禁止行为：外部网络调用、Redis连接、n8n触发、webhook调用、正式库写入、券商接口调用、下单或自动交易、修改被验证文件。

## 封版结论

03进化系统最终规则封版口径为“已封版”。后续新增规则、任务和交付候选必须先通过本封版包的反退化检查；只读验收不构成真实动作授权。
