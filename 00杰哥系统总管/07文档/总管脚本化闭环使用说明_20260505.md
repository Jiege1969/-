# 总管脚本化闭环使用说明

生成时间：2026-05-06 22:15:01

## 使用顺序

1. 运行 `生成总管脚本化闭环汇总器.py`。
2. 运行 `生成一键接续施工包自动刷新.py`。
3. 运行 `验证总管脚本化闭环.py`。
4. 运行 `验证一键接续施工包.py`。
5. 运行 `读取开工上下文.py`。

## 安全边界

这些脚本只读取当前证据并写入00总管最新报告、接续包和上下文摘要；不得写05备份，不得触发 n8n、发送企业微信、调用券商接口或自动交易。
