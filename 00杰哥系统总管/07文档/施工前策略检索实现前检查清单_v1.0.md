# 施工前策略检索实现前检查清单

> 版本：v1.0
> 状态：实现前检查清单，不是生产脚本
> 边界声明：不修改生产脚本，不接自动调用，不写正式库，不触发 n8n 或企业微信。

## 1. 实现前必须确认

| 检查项 | 标准 | 当前状态 |
|:---|:---|:---|
| 风险等级 | 明确脚本或函数属于 R0/W1/W2/W3 | 待实现时确认 |
| 读取范围 | 只读读取治理文件和影子索引 | 草案已列出 |
| 输出格式 | Markdown提醒或JSON提醒 | 草案已列出 |
| 写入行为 | 默认不写入状态 | 必须保持 |
| 自动调用 | 是否接入总管入口 | 待人工确认 |
| 失败处理 | 文件缺失时保守提醒，不自动补文件 | 必须保持 |

## 2. 不得直接实现的部分

- 不直接改最高智能总管入口规则。
- 不直接创建计划任务。
- 不直接接入 n8n。
- 不直接写运行状态或正式库。
- 不把关键词草案当成生产规则。

## 3. 实现前输入材料

- 功能预研主文档。
- 关键词表草案。
- 输出提醒格式草案。
- 读取清单核对报告。
- 当前施工铁律和风险JSON。

## 4. 最小验收建议

```text
只读读取成功
-> 命中关键词
-> 输出提醒
-> 不写任何文件
-> 不触发外部系统
```

## 5. 待补

- 是否需要JSON Schema。
- 是否需要CLI入口。
- 是否纳入最高智能总管三色分流。
