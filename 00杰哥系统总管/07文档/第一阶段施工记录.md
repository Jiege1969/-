# 第一阶段施工记录

## 施工日期

2026-04-26

## 结论

第一阶段已经从准备工作进入实物施工：v3 新系统内已建立一个独立运行的智能体大脑测试服务，端口为 `28100`，用于验证任务识别和模型路由。

## 本次新增

1. 创建 v3 智能体大脑代码目录。
2. 创建任务识别模块。
3. 创建模型路由模块。
4. 创建 FastAPI 服务入口。
5. 创建 ASCII 兼容启动入口 `app.py`。
6. 创建独立 Python venv，并安装 FastAPI 与 uvicorn。
7. 创建启动和停止脚本。
8. 启动本地测试服务 `127.0.0.1:28100`。

## 验证结果

1. 端口 `28100` 已监听。
2. `/health` 返回状态正常。
3. `/任务识别` 测试通过，股票财报估值类输入识别为 `股票研究`。
4. `/模型/选择` 测试通过，代码脚本类输入路由到 `qwen3-coder:30b`，兜底模型为 `qwen3:30b`。

## 风险与边界

1. 本服务只绑定 `127.0.0.1`，不对外开放。
2. 本服务暂不调用生产 Ollama，不接管 n8n，不接管企业微信。
3. 本服务使用新系统目录内 venv，不污染全局 Python。
4. 启动脚本已避开 PowerShell 5 中文路径字符串解析问题。

## 下一步

1. 把模型路由器从静态规则升级为可读取服务状态和模型列表。
2. 接入 Ollama 测试推理，但仍保持非生产端口。
3. 为 `/思考` 增加真实模型调用和超时降级。
4. 增加接口自动化验证脚本。
5. `/思考` 已接入 Ollama 受控推理，后续需要加入更细的提示词模板和任务上下文装配。
