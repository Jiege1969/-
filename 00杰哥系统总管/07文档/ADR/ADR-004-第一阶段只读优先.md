# ADR-004：第一阶段只读优先

日期：2026-04-26

## 状态

已采纳。

## 背景

当前机器上存在老系统、新系统、独立系统和 v3 新骨架。

模型、n8n、数据库、密钥、备份都属于高价值资产。

## 决策

v3 第一阶段以只读盘点、文档落地、配置草案和状态快照为主。

默认不执行：

1. 删除。
2. 覆盖。
3. 移动。
4. 停服务。
5. 重启服务。
6. 迁移模型。
7. 迁移数据库。
8. 读取密钥。

## 后果

1. 可以直接新增 v3 内部文档和只读脚本。
2. 中高风险操作必须先评估。
3. 高风险操作必须等待用户明确确认。

