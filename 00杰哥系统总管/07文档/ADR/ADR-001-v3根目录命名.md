# ADR-001：v3 根目录命名

日期：2026-04-26

## 状态

已采纳。

## 背景

`D:\杰哥智能系统` 已被用户明确说明为另一个独立系统，不能纳入、不能迁移、不能整理、不能删除。

因此 v3 不能使用该路径作为根目录。

## 决策

v3 根目录采用：

```text
D:\杰哥智能化系统
```

## 后果

1. `D:\杰哥智能系统` 写入禁止触碰清单。
2. v3 所有新骨架、档案、脚本、快照均放入 `D:\杰哥智能化系统`。
3. 后续任何操作默认不得触碰 `D:\杰哥智能系统`。

