# 2026-04-26 工作日志

## 今日目标

建立杰哥智能化系统 v3 的初始骨架和基础治理档案。

## 已完成

1. 确认 `D:\杰哥智能系统` 是独立系统，纳入禁止触碰清单。
2. 创建 `D:\杰哥智能化系统` 根目录。
3. 创建四系统骨架：
   - `00杰哥系统总管`
   - `01杰哥智能系统`
   - `02杰哥扩展系统`
   - `03杰哥进化系统`
   - `09迁移暂存`
4. 创建 v3 架构蓝图草案。
5. 创建禁止触碰清单。
6. 创建服务注册表。
7. 创建端口分配表。
8. 创建家底盘点报告草案。
9. 创建迁移路线图草案。
10. 创建组件归属表。
11. 创建模型路由策略。
12. 创建操作安全规范。
13. 复制老系统 7 份非密钥经验文档副本到 v3 历史经验区。
14. 创建历史经验索引。
15. 创建结构化 JSON 配置：服务注册表、端口分配表、模型路由策略、组件归属表。
16. 创建 `只读状态检查.ps1`。
17. 修复 PowerShell 中文编码导致的脚本解析问题，改为 ASCII 安全写法。
18. 成功生成第一份 v3 状态快照，HTTP 检查 8/8 通过。
19. 创建脚本说明文档。
20. 创建 01 主系统设计文档：基础底座、模型路由器、知识库、统一消息出口、任务队列。
21. 创建 01 主系统任务路由规则草案。
22. 创建 02 扩展系统总则和四个业务模块设计文档。
23. 创建 03 进化系统总则和四个进化模块设计文档。
24. 创建模型评测题集草案。
25. 创建 6 份 ADR 架构决策记录。
26. 创建资产迁移映射表。
27. 创建数据资产登记表。
28. 创建迁移前检查清单。
29. 创建备份与恢复策略草案和结构化备份策略 JSON。
30. 创建模块验收标准和结构化验收标准 JSON。
31. 创建健康检查规则草案和说明文档。
32. 创建机器可读配置副本目录 `machine`，用于脚本稳定读取。
33. 创建预搭建自检脚本。
34. 创建目录规范、命名规范、正式搭建前任务清单。
35. 创建文档总索引和根目录 README。
36. 运行预搭建自检，通过 17 项，失败 0 项。
37. 修正预搭建自检脚本中的中文路径输出乱码，改为 ASCII 标签。
38. 再次运行预搭建自检，通过 17 项，失败 0 项。
39. 再次运行只读状态检查，HTTP 检查 8/8 通过。
40. 创建 v3 Compose 草案和环境变量模板。
41. 创建核心 API 契约：网关、大脑、模型路由、消息出口、任务队列。
42. 创建第一阶段施工包。
43. 创建第一阶段代码迁移清单、验证清单、回滚清单。
44. 创建开工判定文档。

## 未执行

1. 未迁移服务。
2. 未复制模型。
3. 未复制数据库。
4. 未读取密钥。
5. 未停止或重启任何容器。
6. 未删除任何旧文件。

## 当前原则

v3 当前只作为架构落地区，不接管生产服务。

## 第一阶段施工补充

45. 创建 v3 智能体大脑测试服务代码目录。
46. 创建任务识别、模型路由和 FastAPI 服务入口。
47. 创建独立 Python venv，安装 FastAPI 与 uvicorn。
48. 创建 v3 智能体大脑测试服务启动和停止脚本。
49. 启动 `127.0.0.1:28100` 测试服务，未接管生产流量。
50. 验证 `/health`、`/任务识别`、`/模型/选择` 三类接口通过。
51. 记录 PowerShell 5 中文编码风险，启动脚本改为 ASCII 自发现路径。
52. 根据用户要求，补强脚本、依赖清单、Compose 草案和环境变量模板的自解释标注。
53. 创建 `脚本依赖命令文件登记表.md`，登记当前脚本、依赖、命令类文件的归属与标注状态。
54. 修正启动脚本端口占用判断，只将 `Listen` 状态视为占用，避免 `TIME_WAIT` 误判。
55. 修正启动脚本 PID 记录逻辑，启动后反查端口监听进程，停止脚本据此停止真实服务进程。
56. 为启动脚本增加已运行服务的 PID 刷新逻辑，避免旧 PID 文件影响后续停止。
57. 创建 v3 智能体大脑接口验证脚本，验证结果写入 v3 日志目录。
58. 为 v3 智能体大脑接入 Ollama 受控推理能力，支持模型调用、输出上限、温度和超时降级。
59. 将真实推理纳入自动验证脚本，要求 `/思考` 返回非空答复。
60. 创建脚本依赖命令文件标注规范检查脚本，将用户提出的规范要求纳入自动检查。
61. 运行标注规范检查并修正依赖清单缺失的依赖字段。
62. 将 `v3智能体大脑测试服务` 登记进服务注册表和端口分配表，端口为 `127.0.0.1:28100`。
63. 将 `v3智能体大脑测试服务` 纳入健康检查规则和只读状态检查脚本。
64. 创建 `智能体大脑规则.json`，将任务识别关键词和模型路由规则从代码剥离到配置。
65. 创建 `运行v3总体验收.ps1`，串联预搭建自检、标注规范检查、只读状态检查和智能体大脑接口验证。
66. 发现总体验收脚本存在自匹配递归问题，已停止递归 PowerShell 进程并修正为排除自身、分步超时执行。
67. 修正总体验收脚本子进程退出码读取逻辑，避免成功步骤被误判失败。
68. 创建 `提示词模板.json` 和 `提示词管理.py`，让不同任务类型使用不同系统提示词。
69. 创建 `能力注册表.json` 和 `能力注册.py`，新增 `/能力/列表` 与 `/能力/规划` 接口，用于生成能力调用计划但不执行外部工具。
70. 将能力规划纳入 v3 智能体大脑验证脚本，总体验收通过 4/4，标注规范通过 17/17。
71. 创建 `当前施工面板.md`，明确 v3 已进入正式搭建第一阶段。
72. 创建知识库数据目录、知识库配置和只读索引清单脚本，第一阶段不迁移旧资料、不写入数据库。
73. 创建 `知识库检索.py`，新增 `/知识库/状态` 与 `/知识库/检索` 接口，当前只读取索引清单。
74. 将知识库状态和检索占位纳入 v3 智能体大脑验证脚本，接口验证通过 9/9，标注规范通过 19/19。
75. 创建知识库入库规则、元数据模板和入库规范文档，明确第一阶段只登记不向量化。
76. 创建 `验证知识库底座.py`，并将知识库底座验证纳入总体验收。
77. 运行总体验收通过 5/5，知识库底座验证通过 6/6，标注规范通过 20/20。
78. 创建 `工作流注册表.json` 和 `工作流规划.py`，新增 `/工作流/列表` 与 `/工作流/规划` 接口，当前只生成计划不触发 n8n。
79. 将工作流规划纳入智能体大脑验证，接口验证通过 10/10，标注规范通过 21/21。
80. 创建股票研究系统第一阶段配置、股票池模板、研究计划脚本和说明文档；当前只生成研究计划，不抓行情、不交易。
81. 创建 `验证股票研究底座.py`，并将股票研究底座验证纳入总体验收。
82. 运行总体验收通过 6/6，股票研究底座验证通过 5/5，标注规范通过 23/23。
83. 创建股票数据源注册表、人工数据快照模板和研究报告生成脚本；当前只读取本地快照，不联网、不交易。
84. 修正股票验证脚本中文输出解码问题，并增强总体验收对 Traceback 和失败字段的识别。
85. 修正总体验收失败字段识别规则，只在失败数大于 0 时判定失败，并修正空失败集合汇总。
86. 修正知识库验证脚本中文输出解码问题，避免子脚本输出导致误判。
87. 重新运行总体验收通过 6/6，知识库底座验证通过 6/6，股票研究底座验证通过 10/10，标注规范通过 24/24。
88. 创建股票报告模板，并让研究报告脚本从模板读取标题、声明和章节规范。
89. 运行股票研究底座验证通过 11/11，总体验收通过 6/6。
90. 创建 `股票研究接口.py`，为智能体大脑新增股票研究状态、计划和报告只读接口。
91. 将股票研究状态、计划和报告接口纳入智能体大脑验证，接口验证通过 13/13，总体验收通过 6/6，标注规范通过 25/25。
92. 创建 `生成股票风险摘要.py`，从最新研究报告中规则化提取风险条目并写入风险摘要。
93. 为智能体大脑新增 `/股票研究/风险摘要` 接口，并在股票任务上下文中附带风险摘要。
94. 运行智能体大脑验证通过 14/14，总体验收通过 6/6，股票研究底座验证通过 14/14，标注规范通过 26/26。
95. 创建视频制作系统第一阶段配置、素材登记模板、脚本模板、分镜模板、任务计划脚本和说明文档。
96. 创建 `验证视频制作底座.py`，并将视频制作底座验证纳入总体验收。
97. 创建 `视频制作接口.py`，为智能体大脑新增视频制作状态、计划、脚本草稿和分镜计划只读接口。
98. 将视频制作只读接口纳入智能体大脑验证，接口验证通过 18/18，总体验收通过 7/7，视频制作底座验证通过 11/11，标注规范通过 29/29。
99. 创建本职工作系统第一阶段配置、办公材料模板、任务计划脚本和说明文档。
100. 创建 `验证本职工作底座.py`，并将本职工作底座验证纳入总体验收。
101. 创建 `本职工作接口.py`，为智能体大脑新增本职工作状态、计划和草稿框架只读接口。
102. 将本职工作只读接口纳入智能体大脑验证，接口验证通过 21/21，总体验收通过 8/8，本职工作底座验证通过 7/7，标注规范通过 32/32。
103. 创建 `05税收业务系统` 目录、税收业务配置、税种分类规则、政策来源、业务处理模板、政策元数据模板、分类计划脚本和说明文档。
104. 创建 `验证税收业务底座.py`，并将税收业务底座验证纳入总体验收。
105. 将税收业务纳入智能体大脑规则、能力注册、提示词模板、工作流注册，并创建 `税收业务接口.py`。
106. 将税收业务接口、任务识别、能力规划和工作流规划纳入智能体大脑验证，接口验证通过 27/27，总体验收通过 9/9，税收业务底座验证通过 10/10，标注规范通过 35/35。
107. 创建税收政策入库规则、税收业务案例模板和税收政策案例索引脚本，支持政策文件与业务案例只读登记。
108. 将税收政策案例索引接入智能体大脑只读接口，新增 `/税收业务/政策案例索引`。
109. 扩展税收业务底座验收，新增政策入库规则、业务案例模板、政策案例索引脚本和最新索引结构检查。
110. 补强税收政策入库规则的质量门槛，补强税收业务案例模板的事实要素和处理结论结构。
111. 重启 v3 智能体大脑测试服务后完成验证：智能体大脑接口 28/28、总体验收 9/9、税收业务底座 15/15、标注规范 36/36。
112. 升级知识库配置，新增全文解析策略，明确 `.md/.txt/.json/.csv/.docx` 直接解析，`.pdf/.xlsx` 暂只登记。
113. 创建知识库内置测试文档 `v3系统说明.md`，用于验证本地全文索引链路。
114. 创建 `生成知识库全文索引.py`，生成清洗文本和全文分块索引，不读取旧系统、不写入向量库。
115. 升级 `知识库检索.py`，知识库状态包含全文索引路径和分块数量，检索优先命中本地全文索引。
116. 扩展知识库底座验收至 10 项，并加严智能体大脑知识库检索验收，要求 `v3` 关键词命中全文索引。
117. 修复知识库检索模块中 `读取最新索引()` 返回空值的问题，重启服务后验证通过：智能体大脑接口 28/28、知识库底座 10/10、总体验收 9/9、标注规范 37/37。
118. 为知识库配置增加元数据伴随文件规则，明确 `原文件名.元数据.json` 的治理方式。
119. 为 `v3系统说明.md` 创建元数据伴随文件，登记文档类型、保密等级、用途范围、关键词和入库状态。
120. 创建 `生成知识库入库批次报告.py`，检查原始文档元数据完整性并生成向量化候选判断。
121. 修正知识库索引清单和全文索引脚本，排除 `.元数据.json` 伴随文件，避免元数据被误当正文资料。
122. 扩展知识库底座验收至 14 项，并完成验证：知识库底座 14/14、智能体大脑接口 28/28、总体验收 9/9、标注规范 38/38。
123. 创建 `n8n接口契约.json`，明确 v3 智能体大脑与 n8n 的入口、出口、禁止字段和执行边界。
124. 创建 `生成n8n工作流草案.py`，根据工作流注册表生成本地 n8n 草案清单，不调用 n8n API。
125. 创建 `验证工作流底座.py`，验证 n8n 接口契约、草案生成、自动触发关闭和未接管状态。
126. 将工作流底座验收纳入 `运行v3总体验收.ps1`，总体验收升级为 10 项。
127. 完成验证：工作流底座 9/9、总体验收 10/10、标注规范 40/40。
128. 创建 `02杰哥扩展系统\00公共组件` 标准结构，作为跨模块统一消息出口承载目录。
129. 创建 `统一消息出口配置.json`，明确唯一出口、优先级、禁止内容和真实发送关闭策略。
130. 创建 `统一消息出口.py`，支持本地消息入队、优先级排序和合并消息预览，不调用企业微信接口。
131. 创建 `验证统一消息出口.py`，验证统一消息配置、本地队列、合并消息和真实发送关闭状态。
132. 修正统一消息出口自检输出编码，避免 Windows 子进程中文 JSON 键被误读。
133. 将统一消息出口验收纳入总体验收，完成验证：统一消息出口 7/7、总体验收 11/11、标注规范 42/42。
134. 创建 `OpenClaw边缘网关契约.json`，明确 OpenClaw 只做企业微信与 n8n/v3 的南北向转发，禁止业务判断和直接发送企微。
135. 创建 `OpenClaw本地回环测试.py`，模拟企业微信消息转换为标准请求，并将模拟响应写入统一消息出口本地队列。
136. 创建 `验证OpenClaw边缘网关.py`，验证契约、本地回环、不连接企业微信、不调用 n8n 和不真实发送。
137. 将 OpenClaw 边缘网关验收纳入总体验收，完成验证：OpenClaw 10/10、总体验收 12/12、标注规范 44/44。
138. 创建 `n8n导入前审查规则.json`，明确导入前条件、禁止导入条件、回滚方案和第一批建议。
139. 创建 `生成n8n导入前审查包.py`，基于工作流草案生成导入候选、风险清单和回滚方案。
140. 创建 `验证n8n导入前审查.py`，验证审查包不调用 n8n、不导入工作流、不启用 Webhook。
141. 将 n8n 导入前审查纳入总体验收，完成验证：n8n 导入前审查 9/9、总体验收 13/13、标注规范 46/46。
142. 创建 `验证真实依赖只读联通.py`，以只读方式验证真实 Ollama、n8n、Redis、PostgreSQL、Docker 容器和 v3 测试大脑联通状态。
143. 将真实依赖只读联通纳入总体验收，单项验证通过 8/8。
144. 补强税收政策入库规则，增加元数据伴随文件规则和全文解析策略。
145. 创建税收政策入库本地测试文档及元数据伴随文件，用于验证政策解析链路，不作为正式税务依据。
146. 更新 `生成税收政策案例索引.py`，排除 `.元数据.json` 伴随文件并登记元数据状态。
147. 创建 `生成税收政策全文索引.py`，生成税收政策清洗文本和全文分块索引，并记录“待核实不得作为正式依据”控制字段。
148. 扩展税收业务底座验收至 19 项，完成验证：税收业务底座 19/19、真实依赖只读联通 8/8、标注规范 48/48、总体验收 14/14。
149. 在知识库配置中增加 Qdrant 写入草案，明确集合名、维度、距离算法和真实写入关闭状态。
150. 创建 `生成知识库向量化前复核清单.py`，读取全文索引和批次报告生成候选点位与阻断点位，不调用 embedding、不连接 Qdrant、不写入向量库。
151. 扩展知识库底座验收至 18 项，完成验证：知识库底座 18/18、标注规范 49/49、总体验收 14/14。
152. 创建 `系统理念配置.json`，把四大系统分工、通用认知循环、顺势治理、防补丁堆叠等理念固化为配置。
153. 创建 `经验分类规则.json`、`方法迁移规则.json`、`治理检查清单.json`，明确同类复用、类似借鉴、异类归纳和人工确认边界。
154. 创建 `生成经验卡片.py` 和 `生成进化复盘报告.py`，支持把施工问题生成经验卡片、方法资产候选和进化建议。
155. 创建 `验证进化系统底座.py`，并将进化系统底座纳入总体验收。
156. 修复 `生成经验卡片.py` 中 Python 布尔值误写为 JSON `true` 的问题，记录为配置语法和代码语法混淆类经验。
157. 完成验证：进化系统底座 10/10、标注规范 52/52、总体验收 15/15。
158. 创建 `生成首批真实经验卡片.py`，把本轮搭建中的真实问题和成功做法生成稳定经验卡片。
159. 首批经验卡片覆盖：脚本自发现递归、Windows 中文 JSON 编码歧义、元数据伴随文件误入正文、先回环后真实联调、高风险资料依据控制、Python/JSON 布尔值混淆。
160. 扩展 `验证进化系统底座.py`，要求首批真实经验卡片不少于 6 张，并覆盖同类复用、类似借鉴、异类归纳三类经验。
161. 完成验证：进化系统底座 13/13、标注规范 53/53、总体验收 15/15。
162. 在系统理念配置中增加“价值提炼优先清理”原则，明确样本不是越多越好。
163. 创建 `样本生命周期规则.json`，定义待复盘、已提炼、已固化、无价值、争议样本等状态和清理前置条件。
164. 创建 `经验价值评分规则.json`，按根因、迁移性、方法资产、高风险边界、固化状态等维度评分。
165. 创建 `生成样本价值评估报告.py`，生成归档候选和清理候选报告，不删除、不移动、不压缩任何样本。
166. 扩展进化系统底座验收至 17 项，完成验证：进化系统底座 17/17、标注规范 54/54、总体验收 15/15。
167. 创建 `提炼通用方法库.py`，从首批真实经验卡片提炼可复用通用方法库候选。
168. 通用方法库覆盖：自动发现边界控制、机器输出与人类文档编码分层、控制数据与业务数据分层、先回环后真实联调、高风险依据分层、跨语法边界检查。
169. 扩展进化系统底座验收至 19 项，完成验证：进化系统底座 19/19、标注规范 55/55、总体验收 15/15。
170. 创建 `税收官方来源核验规则.json`，登记国家税务总局、财政部等官方域名和正式依据候选的最低门槛。
171. 创建 `税收业务案例入库测试.json`，作为本地业务案例索引链路测试资料，不对应真实纳税人和真实交易。
172. 创建 `生成税收业务案例索引.py`，扫描本地业务案例目录，输出字段完整性、政策依据、风险点和人工复核状态。
173. 创建 `核验税收政策官方来源.py`，基于本地域名规则核验政策来源，不联网抓取，不替代人工核实。
174. 创建 `生成税收正式依据过滤结果.py`，综合政策全文索引和官方来源核验结果生成正式依据候选与阻断清单。
175. 扩展 `验证税收业务底座.py` 至 29 项，完成验证：税收业务底座 29/29、标注规范 58/58。
176. 修正 `验证v3智能体大脑测试服务.py` 的 POST 请求等待策略，避免模型冷启动超过 10 秒时误判接口失败；复验智能体大脑 28/28、总体验收 15/15。
177. 创建 `税收真实抓取灰度配置.json`，明确只抓官方来源、只读探测、不下载正文、不写正式政策目录、不写向量库、不触发 n8n、不企微推送。
178. 创建 `生成税收真实抓取灰度计划.py`，生成官方入口只读探测任务、人工确认清单和阻断规则。
179. 创建 `税收官方来源只读探测.py`，对国家税务总局政策法规库、国家税务总局官网、财政部税政司执行只读入口探测，并输出候选链接清单。
180. 创建 `验证税收真实抓取准备.py`，验证灰度配置、只读计划、官方来源探测和禁止入库边界。
181. 将税收真实抓取准备验收纳入总体验收，完成验证：税收真实抓取准备 17/17、标注规范 61/61、总体验收 16/16；本轮官方入口 3/3 连通，候选链接 22 个。
