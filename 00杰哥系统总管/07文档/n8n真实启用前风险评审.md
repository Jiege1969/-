# n8n真实启用前风险评审

生成日期：2026-04-27

## 当前结论

第一批低风险工作流已完成真实 n8n 未激活导入、本地手动测试、回滚预演，当前只具备“进入启用前评审”的资格，不具备自动启用资格。

当前禁止事项：

1. 不批量启用工作流。
2. 不启用Webhook。
3. 不发送企业微信。
4. 不接入税收业务。
5. 不删除旧系统文件。
6. 不迁移生产流量。

## 已导入工作流

| 工作流 | n8n ID | 当前状态 | 已完成闭环 | 关键边界 |
| --- | --- | --- | --- | --- |
| 系统状态巡检 | `UU7kLZDhPvslfNX8` | active=false | 导入、手动测试、回滚预演 | 不外发、不接入税收 |
| 知识库问答增强 | `NR9cqo4kaNZs3mXg` | active=false | 导入、手动测试、回滚预演 | 不写Qdrant/PostgreSQL、不外发 |
| 办公材料生成 | `W5IS0sXoko25QYag` | active=false | 导入、手动测试、回滚预演 | 不生成真实办公材料、不外发 |
| 视频素材处理 | `8A9GjMYqiXyQCjDv` | active=false | 导入、手动测试、回滚预演 | 不复制、不移动、不转码、不调用剪辑软件 |

## 启用顺序建议

1. 系统状态巡检：只做内部状态输出，风险最低。
2. 知识库问答增强：先保持只读检索或模拟响应，不写正式向量库。
3. 办公材料生成：先生成草稿预览，不写入正式文件目录。
4. 视频素材处理：先只读登记素材，不做复制、移动、转码、剪辑。

## 单工作流启用准入

每个工作流启用前必须逐项确认：

1. 触发方式明确，禁止隐式定时触发。
2. 输入来源明确，不读取真实凭据和未授权目录。
3. 输出边界明确，不写正式业务目录。
4. 回滚动作已验证，能强制恢复 active=false。
5. 日志路径明确，能追踪执行结果。
6. 企业微信发送开关关闭。
7. 税收业务保持排除。

## 批量回滚预案

批量回滚只允许做一件事：强制保持 `active=false`。不得删除工作流。

已验证的回滚脚本：

1. `D:\杰哥智能化系统\00杰哥系统总管\02脚本\回滚系统状态巡检n8n未激活导入.py`
2. `D:\杰哥智能化系统\00杰哥系统总管\02脚本\回滚知识库问答增强n8n未激活导入.py`
3. `D:\杰哥智能化系统\00杰哥系统总管\02脚本\回滚办公材料生成n8n未激活导入.py`
4. `D:\杰哥智能化系统\00杰哥系统总管\02脚本\回滚视频素材处理n8n未激活导入.py`

## 下一步

下一步不是直接启用，而是补齐“启用前监控与准入”：

1. 生成单工作流启用检查表。
2. 生成批量 active=false 状态检查脚本。
3. 生成启用后只读观测脚本。
4. 继续保持企业微信真实发送关闭。
