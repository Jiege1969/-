# 00总管 06工具

## 定位

本目录用于放置人工入口、运维入口、可视化工具和辅助工具。

## 边界

1. 可以存放面向用户或施工者的工具脚本、快捷入口、控制台和辅助检查工具。
2. 不存放业务主逻辑。
3. 不存放模型文件、数据库、正式业务数据。
4. 不绕过 `00总管` 的安全闸口直接执行删除、覆盖、真实外发、n8n 正式执行、券商接口或自动交易。

## 与 06临时 的区别

`06工具` 是长期可保留的工具入口；`06临时` 是可清理的中间材料和短期试验区。二者同为 `06` 是历史例外，读取时必须按完整目录名判断。

