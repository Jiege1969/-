# 防火墙与环境变量死路径清理收口

时间：2026-05-06 07:14:00 +08:00

## 一、处理结论

已清理死路径防火墙规则 13 条，并修正环境变量死路径。第十三轮覆盖核实：此前保留的 `OpenClaw 8080` 入站规则已在第十一轮删除；当前规则数 0、8080 不监听、portproxy 为空。

## 二、删除的防火墙规则

删除对象均满足：规则仍启用，但目标程序路径不存在，且不属于当前 `杰哥智能化系统_*` 主线入口。

- 旧 Node.js 规则：2 条，指向 `D:\杰哥智能系统\工具软件\nodejs\node.exe`。
- 旧腾讯 QClaw 规则：2 条，指向 `D:\杰哥智能系统\qclaw\qclaw.exe`。
- 旧企业微信 wemail/wemailnode 规则：8 条，指向不存在的 `E:\企业微信\WXWork\5.0.7.6005\...` 或乱码旧路径 `E:\ҵ΢\WXWork\5.0.8.6009\...`。
- 重复 `OpenClaw 8080` 规则：本报告生成时删除 1 条、曾残留 1 条；第十一轮已继续删除剩余 1 条，当前 `OpenClaw 8080` 规则数为 0。

## 三、保留对象

- 现役 Node.js 规则保留：指向 `C:\program files\nodejs\node.exe`，程序存在。
- `OpenClaw 8080` 已从当前对象中移除；当前 OpenClaw 入口为 `127.0.0.1:18789`，旁路端口为 `18791/18792`。
- Docker Desktop 启动项、Docker hosts 条目、`杰哥智能化系统_*` 计划任务未触碰。

## 四、环境变量修正

- 机器级 `CLAUDE_CODE_GIT_BASH_PATH` 已从不存在的 `D:\00杰哥系统总管\99外部工具\Git\bin\bash.exe` 修正为：

`D:\杰哥智能化系统\01杰哥智能系统\03数据\本机工具运行时\工具软件\Git\bin\bash.exe`

- User `Path` 中不存在的 Ollama 路径已移除：
  - `C:\Users\Administrator\AppData\Local\Programs\Ollama`
  - `D:\Programs\Ollama`

## 五、证据与验收

证据目录：

`第30轮原始暗角证据目录已清债删除；判断结论、删除对象、验收结果和安全边界已固化在对应_latest收口报告。`

验收结果：

- 13 条目标规则均不存在。
- 保留的 Node.js 规则目标程序存在。
- `OpenClaw 8080` 规则已被第十一轮覆盖清零：剩余 0 条，8080 不监听。
- `CLAUDE_CODE_GIT_BASH_PATH` 目标存在。
- User `Path` 中无 Ollama 死路径条目。

## 六、安全边界

未触发 n8n，未发送企业微信，未调用券商接口，未自动交易，未删除正式数据，未删除 `杰哥智能化系统_*` 计划任务。

## 第30轮原始证据清债说明

- 第30轮原始暗角证据目录已清债删除；判断结论、删除对象、验收结果和安全边界已固化在对应_latest收口报告。

