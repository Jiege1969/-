# 全系统运行态总检与总纲对齐审计

生成时间：2026-05-05 22:05

## 总结论

- 当前系统不是“没搭完”，而是进入了交付后运行态校准阶段。
- 总纲中的交付口径基本成立：股票系统、总管入口、智能底座、企业微信回调、开机自检、非交易闸门均可用。
- 真实运行中发现的偏差主要是运行口径漂移：旧启动项残留、企业微信长连接/回调口径混写、云端反向隧道短时断开、总纲部分早期基线未同步刷新。
- 本轮已直接处理能判断的项：删除旧启动项、禁用长连接运行口径、恢复回调单通道口径、清理临时残留。

## 已走通

| 模块 | 当前事实 |
|---|---|
| 股票助手 | `19300` 健康检查 200，仍为分析系统，不接券商、不下单、不自动交易 |
| 股票企微桥接 | `19302` 健康检查 200，公网隧道已恢复 |
| 总管统一入口 | `19310` 健康检查 200，可处理系统状态、ping、帮助等确定性指令 |
| v3智能体大脑 | `28100` 健康检查 200；当前为父子进程结构，监听进程正常 |
| n8n | `28679/healthz` 200；正式执行仍关闭 |
| Ollama | `29134/api/tags` 200，可见模型接口 |
| Redis | `26379` 容器运行，`PONG` |
| 公网回调 | `/wecom/system-manager`、`/wecom/work-secretary`、`/wecom/video-assistant` 均 200 |
| 开机提示 | 右下角启动提示可读取自检结果并自动退出 |
| 启动项 | 仅保留 `杰哥智能化系统_*` 主线计划任务，均走 `wscript.exe` 隐藏包装 |

## 已纠偏

- 删除旧启动项 `JiegeAgentOS_AutoStart`。
- 移除 `启动企业微信统一指令本地服务.ps1` 中的长连接自动启动逻辑。
- 将 `企业微信双通道接入口径.json` 改为公网 URL 回调主用。
- 将 `企业微信智能机器人长连接路由表.json` 改为禁用历史留痕。
- 清理 PyInstaller 打包缓存、脚本 `__pycache__`、启动验证临时日志、长连接 pid/空日志，合计约 `15.8MB`。

## 总纲与现实差距

| 差距 | 判断 | 处理 |
|---|---|---|
| 总纲早期基线仍写 `19310/28100` 未监听 | 过期口径 | 已在本报告和启动项目检查中刷新为运行中 |
| 企业微信文档曾写长连接主用 | 错误口径 | 已改为回调主用、长连接禁用 |
| 企业微信 21:26-21:33 未回话 | 运行链路短时故障 | 云端日志显示 `18080` 反向隧道断开导致 502；当前恢复 |
| 旧 Docker 容器仍存在退出态 | 退役残留，不影响当前运行 | 暂不删除数据容器；后续做旧容器退役包 |
| n8n/Redis/视频/内容/税收多为影子或门禁态 | 设计使然，不是失败 | 继续保持真实动作关闭，等许可令 |
| 操作说明分散在多个报告中 | 交付体验缺口 | 已生成操作说明底稿，后续拆成手机端短文档 |

## 当前不应删除

- `.venv`、模型目录、Docker 数据目录、n8n 数据、Redis 数据、股票正式数据、备份归档。
- 旧 `jiege_*` Docker 容器的挂载数据仍有历史引用，不在本轮直接删除。

## 下一步

1. 将本报告同步到全盘架构说明的最新运行态章节。
2. 继续整理 5 篇手机可读操作卡：开机状态、企业微信机器人、股票分析、故障排查、备份恢复。
3. 做旧 Docker 容器退役候选包：先列证据、再区分可删容器和需保留数据。
4. 给公网反向隧道增加守护巡检：发现 `18080` 不通时自动重拉隧道并记录，不触发真实业务动作。

## 安全边界

本轮未触发 n8n 正式执行，未发送企业微信消息，未调用券商接口，未自动交易，未删除业务数据、模型、数据库、n8n 或备份归档。
