# 企业微信系统管家回调密钥同步与 URL 校验

生成时间：2026-05-05 19:59

## 本轮处理

根据企业微信后台生成的【杰哥系统管家】回调配置，已完成本机私密配置同步和公网回调校验适配。

## 已同步

- URL：已登记为系统管家公网回调入口。
- Token：已写入本机私密配置，不在日志和普通报告中展开。
- EncodingAESKey：已写入本机私密配置，不在日志和普通报告中展开。
- 写入前已备份原私密配置到 `05备份/私密配置`。

## 已改造

- `19302` 公网桥接对 `/wecom/system-manager` 使用系统管家专用 Token/AESKey 校验。
- 支持企业微信后台保存 URL 时的 `echostr` 解密校验。
- 支持系统管家加密回调消息解密后转发到本机 `19310`。
- 保持股票原 `/wecom-bot/message` 路径不变。

## 验收

- 公网 `GET /wecom/system-manager?text=ping`：通过，返回 `system-manager-ping`。
- 公网 `POST /wecom/system-manager text=系统状态`：通过，返回 `system-manager-status`。
- 企业微信 `echostr` 加密校验模拟：通过。
- 股票公网回归：通过，`/wecom-bot/message` 仍能返回股票分析。

## 安全边界

本轮未触发 n8n，未发送真实企业微信消息，未调用券商接口，未交易，未删除数据。

## 提醒

桌面临时文件中包含企业微信回调密钥，建议绑定成功后转入安全保存或清理。

