# 摸清家底找差距第四十三轮家底制度化维护收口

时间：2026-05-06 20:37:00 +08:00

## 一、处理结论

本轮把补充建议十至十三先落到总管和备份体系：数据资产台账、系统依赖与恢复清单、暗角落扫描和家底制度化维护包均已生成最新锚点。

- 数据资产：19 类不可再生资产全部具备 D 盘主副本 + F 盘当前高价值小资产快照，双副本状态 0 个需复核。
- 依赖恢复：Docker、WSL、Python、Node、端口、计划任务、环境变量、敏感配置位置已写入 `F:\系统备份\依赖清单\系统依赖与恢复清单.md`。
- 暗角扫描：计划任务、启动项、环境变量死路径、hosts、防火墙、WSL crontab/bashrc 已只读扫描；风险线索 0，环境变量死路径 0。
- 文本洁净：四个关键 Markdown 输出的 NUL、VT、替换字符均为 0。

## 二、已固化能力

- 新增脚本：`D:\杰哥智能化系统\00杰哥系统总管\02脚本\生成家底制度化维护包.py`
- 数据资产台账：`F:\系统备份\数据资产台账\数据资产台账_最新.md`
- 依赖恢复清单：`F:\系统备份\依赖清单\系统依赖与恢复清单.md`
- 暗角扫描报告：`D:\杰哥智能化系统\00杰哥系统总管\03数据\运行状态\暗角扫描\暗角落只读扫描报告_最新.md`
- 维护总包：`D:\杰哥智能化系统\00杰哥系统总管\03数据\运行状态\家底制度化维护包_最新.md`

## 三、纠偏记录

本轮发现一次脚本口径风险：最初把 `01杰哥智能系统\03数据` 整体纳入当前高价值小资产快照，导致 `ollama` 可再生模型目录被误拷贝到 F 盘快照。

已立即处理：

- 终止对应的两个 `生成家底制度化维护包.py` 进程。
- 删除 F 盘当前快照中的误拷贝目录：`F:\系统备份\当前高价值小资产快照_20260506_153620\01智能_03数据\ollama`。
- 保留 D 盘模型本体：`D:\杰哥智能化系统\01杰哥智能系统\03数据\ollama`。
- 脚本改为只同步 `n8n` 和 `知识库` 等不可再生小资产，并加入 1GB 上限闸门。

## 四、规则吸收

- 不可再生资产必须有 D+F 双副本。
- 可再生大件只写恢复依据，不进入小资产快照。
- 暗角扫描必须读取 User/Machine 持久状态，不以当前进程环境快照作为系统事实。
- 发现误处理要直接纠正并把防复发逻辑写进脚本，而不是登记成新待办。

## 五、源头复核

并行核实窗口曾提示部分脚本可能仍按时间戳旧流水取“最新”。本轮复核当前代码后确认：

- `生成日常可用版阶段报告.py`：只读取固定 `*-最新.json`。
- `生成基础可用版完成判定报告.py`：只读取固定 `*-最新.json`。
- `执行当前低风险自动推进队列.py`：只读取 `stock-delivery-total-acceptance-最新.json`，不存在时闭口返回空，不回退旧流水。
- `知识库检索.py`：只读取 `知识库索引清单_最新.json` 和 `知识库全文索引_最新.json`；“全文未命中回退元数据”不是文件版本回退。

## 六、安全边界

未触发 n8n，未发送企业微信，未调用券商接口，未自动交易，未删除正式数据。删除对象仅限 F 盘当前快照中的误拷贝可再生模型副本，且 D 盘模型本体已核实存在。
