# 启动项目检查

生成时间：2026-05-05 21:58

## 结论

- 右下角启动提示：已验证，读取开机自检结果后可自动退出。
- 开机自检：通过，`ready_for_codex=true`，`warnings=[]`。
- 当前关键入口：`19300` 股票助手、`19302` 企业微信公网桥接、`19310` 总管统一入口、`28679` n8n、`29134` Ollama 均在监听。
- Docker 基础组件：`jiege_v3_ollama`、`jiege_v3_redis`、`jiege_v3_n8n` 均在运行。
- 当前系统启动项：只保留 `杰哥智能化系统_*` 主线任务，均使用 `wscript.exe` 隐藏包装。
- 旧启动项 `JiegeAgentOS_AutoStart` 已删除。

## 本轮修正

- `启动企业微信统一指令本地服务.ps1` 已改为只启动本地19310统一回调入口，不再启动企业微信长连接客户端。
- 企业微信接入口径已改为公网URL回调主用，长连接为禁用历史留痕。
- `start_stock_runtime_entry.ps1` 已改为限时、隐藏、可退出；验证后计划任务不再长期停留在 Running。
- 已停止后台长连接进程，避免 websocket 认证失败空转。

## 机器人未回话判断

- 云端日志显示，2026-05-05 21:26 至 21:33 企业微信请求到达公网，但当时云端 `127.0.0.1:18080` 反向隧道未连上，返回 502。
- 当前反向隧道已恢复，`/wecom/system-manager`、`/wecom/work-secretary`、`/wecom/video-assistant` 公网路径均返回 200。
- 股票公网回调加密闭环验证：4/4 通过。
- 股票企微前台交互回归验证：41/41 通过。

## 安全边界

本轮未触发 n8n 正式执行，未发送企业微信消息，未调用券商接口，未自动交易，未删除业务数据。
