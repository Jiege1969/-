# 知识库问答灰度许可接收清单影子模板

- 生成时间：2026-05-05 06:53:52
- 结论：通过
- 模板结论：已生成空许可接收清单模板，但未填写许可，禁止灰度
- 前置暂停闸口通过：True

## 字段

- 许可编号：默认 ``；必填：True；人工填写，机器不得生成
- 授权人：默认 ``；必填：True；人工填写
- 授权时间：默认 ``；必填：True；人工填写
- 灰度范围：默认 ``；必填：True；例如只读问答、指定成员、指定样本
- 样本来源：默认 ``；必填：True；人工确认样本来源
- 样本数量上限：默认 ``；必填：True；必须为人工指定数字
- 回滚确认：默认 ``；必填：True；必须确认回滚对象和触发条件
- 企业微信真实发送许可：默认 `否`；必填：True；默认否
- n8n触发许可：默认 `否`；必填：True；默认否
- 正式入口接入许可：默认 `否`；必填：True；默认否，且仍需停下报告

## 填写规则

- 所有必填字段必须由用户或人工流程明确填写。
- 机器不得代填授权人、授权时间、灰度范围、样本来源、样本数量上限或回滚确认。
- 企业微信真实发送、n8n触发、正式入口接入默认均为否。
- 正式入口接入即使被人工写为是，也仍必须停下报告，不能由本模板自动放行。

## 安全边界

本轮只生成空许可接收清单影子模板，不填写许可，不填真实样本，不放行灰度，不接入正式入口，不调用企业微信，不触发 Webhook/n8n，不写库。
