# 个人智能母系统服务恢复后运行基线

- 生成时间：2026-05-04 13:47:06 +08:00
- 所属系统：00 杰哥系统总管
- 安全边界：只记录恢复后的运行基线；不触发 n8n、不发送企业微信、不写业务库、不调用券商接口、不自动交易。

## 当前结论

`19310` 企业微信统一指令本地服务和 `28100` v3 智能体大脑测试服务已恢复。股票入口、企业微信桥接、统一指令本地服务、v3 智能体大脑测试服务、v3 n8n、v3 Ollama、v3 Redis 均可用。

总管日常调度状态为“用户重负载避让”：当前入口保持可用，但后台重任务、批量模型推理、备份压缩、版本升级和新业务扩展应暂缓。

## 入口状态

| 服务 | 地址或端口 | 状态 | 说明 |
|---|---|---|---|
| 股票助手入口 | `http://127.0.0.1:19300/health` | 正常 | 重点关注池数量 123；旧系统写入 false；交易接口 false |
| 股票企业微信桥接入口 | `http://127.0.0.1:19302/health` | 正常 | 不写旧系统、不调用券商接口、不自动交易 |
| 企业微信统一指令本地服务 | `http://127.0.0.1:19310/health` | 正常 | 仅本地预演；不真实发送企业微信、不触发 Webhook、不触发 n8n |
| v3 智能体大脑测试服务 | `http://127.0.0.1:28100/health` | 正常 | 仅用于 v3 测试，不接管旧系统生产流量 |
| v3 n8n | `http://127.0.0.1:28679/healthz` | 正常 | 返回 `{"status":"ok"}` |
| v3 Ollama | `http://127.0.0.1:29134/api/tags` | 正常 | 可见 14 个模型 |
| v3 Redis | `127.0.0.1:26379` | 正常 | `redis-cli ping` 返回 `PONG` |

## 调度与资源

| 项目 | 当前结果 |
|---|---|
| 总管日常调度状态 | 用户重负载避让 |
| 任务准入 | 当前只读巡检允许执行 |
| 队列调度 | 4 个任务中允许 3、禁止 1；触发执行器 0；触发 n8n 0 |
| CPU | 约 8% |
| 内存 | 总内存约 64570 MB，空闲约 43369 MB |
| GPU | RTX 3090，显存占用约 20403/24576 MB，约 83.02%；GPU 利用率约 34% |
| Docker v3 容器 | v3 Ollama、v3 Redis、v3 n8n 均运行，资源占用较低 |

## 当前执行策略

1. 保持股票入口和企业微信入口可用。
2. 后台重任务暂缓，尤其是批量模型推理、模型下载、视频渲染、备份压缩和版本升级。
3. 真实发送企业微信、触发 n8n 正式工作流、写正式库、调用券商接口、自动交易继续禁止。
4. 后续如果处理旧容器、注册表写回或服务自启，需要单独验收并回写总架构说明。
