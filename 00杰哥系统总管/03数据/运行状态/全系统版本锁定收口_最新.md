# 全系统版本锁定收口

记录时间：2026-05-06 05:53:00 +08:00

## 一、本次目标

落实第二轮并行核实 P1 项：把当前正式运行版本、关键依赖、容器镜像、模型标签和升级窗口规则固化为台账，后续升级、回滚和故障排查不得凭记忆。

## 二、新增产物

- `D:\杰哥智能化系统\00杰哥系统总管\03数据\运行状态\全系统版本锁定表_最新.md`
- `D:\杰哥智能化系统\00杰哥系统总管\03数据\运行状态\全系统版本锁定表_最新.json`

## 三、当前正式运行栈

当前可锁定为：

`Python 3.14.4 + pip 26.0.1 + Node v25.9.0 + npm 11.12.1 + Docker 29.3.1 + Docker Compose v5.1.1 + Ollama 0.20.5 + Redis 7.2.5-alpine + n8n 1.123.30`

正式运行容器：

- `jiege_v3_ollama`：`ollama/ollama:0.20.5`
- `jiege_v3_redis`：`redis:7.2.5-alpine`
- `jiege_v3_n8n`：`n8nio/n8n:1.123.30`

## 四、关键风险

1. Python、Node、Docker/Compose 都偏新，不能和业务组件同窗升级。
2. 全局 `pip` 命令不可直接用，统一使用 `python -m pip`。
3. 部分 Python 包依赖来自全局环境，后续需要按项目 venv 冻结。
4. 模型中仍有 `bge-m3:latest`、`tinyllama:latest`、`glm4:latest`，不得作为正式不可回滚标签使用。
5. 未运行的 `latest` 镜像和旧本地构建镜像只进入清理候选，不进入正式 compose。

## 五、验收结果

| 项目 | 结果 |
|---|---|
| 版本锁定 JSON 可解析 | 通过 |
| 版本锁定 Markdown 已生成 | 通过 |
| 正式容器镜像已列出 | 通过 |
| 模型 latest 风险已标注 | 通过 |
| 升级铁律已写入 | 通过 |

## 六、安全边界

本轮只读采集和写台账：

- 未安装任何包。
- 未升级任何组件。
- 未重启服务。
- 未触发 n8n。
- 未发送企业微信。
- 未调用券商接口。
- 未自动交易。

## 七、结论

版本锁定表已完成。下一步可以进入 Docker 旧卷旧镜像清理预案，但仍然只做预案和归属复核，不直接 prune。

