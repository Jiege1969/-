# 本轮并行施工派工包

更新时间：2026-05-06 13:30 +08:00

## 当前目标

第十五轮继续执行摸清家底找差距清债工作，重点清理当前入口、规则库、接续包和运行状态中的旧债壳、乱码、旧口径误导。

## 并行分工

- 00 总管：核查当前入口、开工上下文、规则文件、验收报告和运行状态 `_最新` 文件。
- 01 智能系统：仅作为当前依赖资产核查对象，不新增功能，不切换模型，不触发推理链路。
- 02 扩展系统：以股票系统为母样本提纯规则，不复制旧债；税收等新系统只继承结构、边界、验收和防复发逻辑。
- 03 进化系统：沉淀防复发规则，不沉淀坏状态本身。

## 统一规则

- 无依赖旧项当轮删除。
- 有当前依赖项修正为资产登记。
- 历史流水不进入当前入口。
- 发现处理错误立即纠正。

## 安全边界

不触发 n8n，不发送企业微信，不调用券商接口，不自动交易，不删除不可再生数据，不删除当前依赖资产。
