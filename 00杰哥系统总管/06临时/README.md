# 00总管 06临时

## 定位

本目录用于放置中间文件、候选清理材料、短期试验材料和临时接续材料。

## 边界

1. 这里的文件默认不是正式口径。
2. 临时文件清理前必须确认是否已转为报告、台账、经验卡片或正式文档。
3. 不存放模型文件、数据库、正式业务数据。
4. 不作为长期运行入口，不注册计划任务指向本目录。

## 与 06工具 的区别

`06临时` 是候选清理区；`06工具` 是长期工具入口区。二者同为 `06` 是历史例外，短期不搬目录，靠 README 和索引明确边界。

