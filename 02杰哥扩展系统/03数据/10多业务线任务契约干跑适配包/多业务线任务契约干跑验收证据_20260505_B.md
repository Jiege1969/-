# 02扩展系统多业务线任务契约干跑适配包验收证据

- 任务：并行小任务 B，02扩展系统多业务线任务契约干跑适配
- 生成时间：2026-05-05 13:35 +08:00
- 执行模式：dry_run_only，本地只读验收
- 真实动作数量：0
- 本职工作系统：暂停，不搭建，不触碰

## 产物路径

| 类型 | 路径 | 说明 |
| --- | --- | --- |
| 契约 JSON | `D:\杰哥智能化系统\02杰哥扩展系统\03数据\10多业务线任务契约干跑适配包\多业务线任务契约干跑适配包_20260505_B.json` | 五条扩展线的标准任务单、receipt、影子 SQLite/Redis/n8n 干跑描述 |
| 验收 Markdown | `D:\杰哥智能化系统\02杰哥扩展系统\03数据\10多业务线任务契约干跑适配包\多业务线任务契约干跑验收证据_20260505_B.md` | 本文件 |
| 验证脚本 | `D:\杰哥智能化系统\02杰哥扩展系统\02脚本\验证多业务线任务契约干跑适配包.py` | 只读解析 JSON，不连接网络、SQLite、Redis、n8n |
| 回收报告 Markdown | `D:\杰哥智能化系统\00杰哥系统总管\03数据\并行回收\02扩展系统_轻量任务契约多业务线回收报告_最新.md` | 本轮固定回收报告 |
| 回收报告 JSON | `D:\杰哥智能化系统\00杰哥系统总管\03数据\并行回收\02扩展系统_轻量任务契约多业务线回收报告_最新.json` | 机器可读回收报告 |

## 多业务线接收与回执

| 业务线 | 标准任务单接收方式 | dry_run 保持 | receipt 输出 | 真实动作阻断 |
| --- | --- | --- | --- | --- |
| 企业微信 | `business_line=enterprise_wechat`，接收消息草稿和影子目标 | 必须 `dry_run=true` | 消息预览、影子路由、阻断发送原因 | 阻断企业微信真实发送、response_url、webhook、n8n |
| 知识库 | `business_line=knowledge_base`，接收问题和本地来源候选 | 必须 `dry_run=true` | 来源索引预览、引用状态、不可回答边界 | 阻断正式知识库、向量库、正式数据库写入 |
| 内容处理 | `business_line=content_processing`，接收样例内容和影子格式目标 | 必须 `dry_run=true` | 解析预览、格式计划、转换阻断原因 | 阻断真实转换、覆盖/移动/删除用户文档、发布 |
| 视频制作 | `business_line=video_production`，接收脚本提纲和影子素材清单 | 必须 `dry_run=true` | 制作计划、素材缺口、渲染阻断原因 | 阻断转码、剪辑、渲染、字幕/封面成品、发布 |
| 税收只读 | `business_line=tax_readonly`，接收政策来源候选和税务问题影子输入 | 必须 `dry_run=true` | 政策来源候选、人工复核字段、不可判断边界 | 阻断正式税务判断、申报建议、自动申报、正式税务库 |

## 影子基础设施边界

- SQLite：仅声明 `shadow_ledger_plan.sqlite` 计划路径，`connect=false`，`write=false`，不创建库、不执行 SQL。
- Redis：仅声明 `dryrun:ext:task_contract:` key 前缀，`connect=false`，`write=false`，不 SET/DEL/PUBLISH。
- n8n：仅保留干跑描述，`trigger_n8n=false`，`webhook_url=null`，不激活 workflow。
- OpenClaw/Hermes：只学习任务路由、receipt、影子台账逻辑，不引入原件或原始实现。

## 阻断项

- 阻断 n8n 触发。
- 阻断企业微信真实发送。
- 阻断正式知识库、正式向量库、正式数据库写入。
- 阻断真实内容转换、真实媒体转换、真实视频渲染。
- 阻断正式税务适用判断、正式申报建议、自动申报。
- 阻断券商接口、自动交易、下单、撤单。
- 阻断本职工作系统搭建或触碰。

## 验收检查项

- 五条业务线均存在适配器：企业微信、知识库、内容处理、视频制作、税收只读。
- 所有样例标准任务单均为 `dry_run=true` 且 `receipt_required=true`。
- 所有样例 receipt 均为 `dry_run=true`，包含 shadow ledger、Redis shadow key、n8n dry-run ref。
- SQLite/Redis 均为影子计划且 `connect=false`、`write=false`。
- n8n 为 descriptor only，`trigger_n8n=false`，`webhook_url=null`。
- 全局阻断项覆盖真实发送、正式库、真实转换、正式税务判断、券商接口、自动交易。

## 安全边界

本适配包只作为可验收的本地 JSON/Markdown 证据和只读解析脚本使用，不构成生产系统配置，不提供真实 webhook，不连接 SQLite/Redis/n8n，不触发企业微信、正式库、媒体工具链、税务系统、券商接口或自动交易。
