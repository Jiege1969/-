# 02扩展系统受控真实灰度许可令草案包

- 生成时间：2026-05-05 14:18 +08:00
- 小任务：并行小任务I
- 范围：企业微信、知识库、内容、视频、税收只读五条扩展线
- 状态：草案，仅供人工复核与后续审批使用
- 执行边界：不导入、不启用、不触发 workflow；不调用外部系统；不执行真实发送、正式写入、真实转换、正式税务判断、券商接口或自动交易
- 本职工作系统：暂停，不碰
- 真实动作数量：0

## 全局许可边界

未来可进入低风险正式候选的动作仅限：

| 候选动作 | 约束 |
| --- | --- |
| 只读拉取 | 只能读取白名单内样本、索引、目录或元数据，不得写入、覆盖、发布或外发 |
| 草稿生成 | 只能生成影子草稿、脚本草稿、知识卡草稿或内部清单草稿，不得进入正式库或生产队列 |
| 内部报告流转 | 只能在内部复核链路流转，不得对外发送、不得形成自动结论 |

继续禁止的动作：

| 禁止动作 | 适用范围 |
| --- | --- |
| 企业微信放量 | 企业微信 |
| 正式库写入 | 知识库、内容、税收、任何生产数据库 |
| 真实媒体转换 | 视频、内容 |
| 税务正式判断 | 税收只读 |
| 券商接口 | 全扩展系统 |
| 自动交易 | 全扩展系统 |
| n8n 导入、启用或触发 | 全扩展系统 |
| 外部 webhook 或生产队列写入 | 全扩展系统 |

## 五条线许可令草案

| 业务线 | 未来低风险正式候选 | 白名单草案 | 回滚草案 | 监控草案 | 人工复核字段 |
| --- | --- | --- | --- | --- | --- |
| 企业微信 | 只读拉取沙箱会话元数据；生成不发送回复草稿；内部监控报告流转 | 沙箱会话元数据、内部测试用户别名、影子回复预览记录 | 发现外部发送意图、白名单不明、生产队列写入迹象时，保持发送关闭并废弃草稿批次 | 草稿数、只读拉取数、阻断发送尝试数、白名单未命中数 | 会话范围、消息样例 ID、发送通道关闭证据、复核人确认无外发 |
| 知识库 | 只读拉取知识库索引；生成不入库知识卡草稿；内部证据链报告流转 | 只读索引快照、证据引用样本、草稿知识卡 | 出现写入或 upsert 意图、证据缺失、无法追溯时，废弃草稿并保持正式库只读 | 只读索引拉取数、草稿知识卡数、阻断写入尝试数、证据缺失数 | 源文档 ID、证据片段、只读快照 ID、复核人确认无正式写入 |
| 内容 | 只读拉取样本文档元数据；生成不发布内容草稿；内部质量报告流转 | 样本文档元数据、影子内容草稿、内部质量报告 | 发现发布意图或正式内容库写入意图时，作废草稿并冻结候选白名单 | 样本读取数、草稿生成数、阻断发布尝试数、阻断库写入数 | 样本文档 ID、草稿输出 ID、来源追溯证据、复核人确认无发布 |
| 视频 | 只读拉取素材清单；生成不转码脚本或分镜草稿；内部制作报告流转 | 只读素材清单、脚本草稿、分镜草稿、内部制作报告 | 发现转码、渲染、发布或误入成品目录时，废弃草稿并保持媒体队列关闭 | 素材清单读取数、脚本草稿数、阻断转码尝试数、阻断渲染尝试数 | 素材清单 ID、脚本草稿 ID、媒体队列关闭证据、复核人确认无真实媒体转换 |
| 税收只读 | 只读拉取税收资料目录；生成不构成判断的材料清单草稿；内部复核报告流转 | 税收资料目录只读记录、内部材料清单草稿、内部复核报告 | 出现税务结论、申报建议、税务 API 调用或正式库写入意图时，作废草稿并标记非税务意见 | 只读目录拉取数、清单草稿数、阻断税务判断数、阻断税务 API 尝试数 | 税收资料目录 ID、非税务意见声明、税务接口关闭证据、复核人确认无正式税务判断 |

## 人工复核字段

全局必填字段共 21 项：permission_order_id、business_line、requested_action、risk_level、readonly_or_draft_scope、whitelist_reference、rollback_reference、monitoring_reference、evidence_reference、operator、reviewer、approver、approval_ticket、start_time、end_time、kill_switch_owner、rollback_owner、data_write_flag、external_call_flag、forbidden_action_scan_result、final_human_review_result。

任一字段缺失，本草案包不得进入正式审批；任一禁止动作命中，本草案包必须回滚为“草案废弃”。

## 验收口径

- 五条扩展线均已覆盖许可令草案、白名单、回滚、监控、人工复核字段。
- 已明确未来低风险正式候选仅为只读拉取、草稿生成、内部报告流转。
- 已明确继续禁止企业微信放量、正式库写入、真实媒体转换、税务正式判断、券商接口、自动交易。
- 本轮只写草案和只读验证脚本，不执行真实动作。
