# 02扩展系统低风险小样本许可令只读签发草案

- 生成时间：2026-05-05 14:53 +08:00
- 小任务：并行小任务Q
- 来源：基于 `受控真实灰度许可令草案包_20260505_I` 收窄
- 状态：只读签发草案，仅供人工复核与后续审批使用
- 真实动作数量：0
- n8n：未导入、未启用、未正式触发

## 签发边界

本草案不构成正式授权。未来候选只允许三类：

| 候选动作 | 边界 |
| --- | --- |
| 只读拉取 | 仅限后续二次审批后读取小样本白名单内样本、索引、目录或元数据；不得写入、覆盖、发布、外发或触发生产队列 |
| 草稿生成 | 仅限后续二次审批后生成影子草稿、脚本草稿、知识卡草稿或内部清单草稿；不得进入正式库或形成正式业务结论 |
| 内部报告流转 | 仅限内部复核链路流转小样本观察报告、证据链报告或质量报告；不得对外发送或自动签收为正式结论 |

## 继续禁止

| 禁止项 | 明确口径 |
| --- | --- |
| 企业微信放量 | 禁止真实群发、单发、response_url、机器人 webhook、正式消息队列写入 |
| n8n正式触发 | 禁止导入、启用、正式触发或手动执行任何 n8n workflow |
| 正式库写入 | 禁止知识库、内容库、税务库、向量库、生产数据库或生产队列写入 |
| 真实媒体转换 | 禁止转码、剪辑、渲染、字幕封面成品生成或发布 |
| 正式税务判断 | 禁止申报建议、自动申报、正式税务意见、税务 API 调用或对外结论出具 |
| 券商接口 | 禁止调用任何券商查询、下单、撤单、账户或行情交易接口 |
| 自动交易 | 禁止自动下单、调仓、撤单、跟单或任何交易执行链路 |

## 覆盖线

| 业务线 | 只读签发范围 | 后续使用前证据 |
| --- | --- | --- |
| 企业微信 | 只读拉取沙箱会话元数据、生成不发送回复草稿、内部监控报告流转 | 发送通道关闭证据、小样本白名单编号、人工二人复核记录 |
| 知识库 | 只读拉取索引或证据样例、生成不入库知识卡草稿、内部证据链报告流转 | 只读快照编号、证据引用范围、人工二人复核记录 |
| 内容 | 只读拉取样本文档元数据、生成不发布内容草稿、内部质量报告流转 | 样本文档编号、来源追溯证据、发布通道关闭证据 |
| 视频 | 只读拉取素材清单、生成不转码脚本或分镜草稿、内部制作报告流转 | 素材清单只读编号、媒体队列关闭证据、复核人确认无真实媒体转换 |
| 税收只读 | 只读拉取税收资料目录、生成不构成判断的材料清单草稿、内部复核报告流转 | 非税务意见声明、税务接口关闭证据、复核人确认无正式税务判断 |
| 股票研究边界 | 仅保留内部只读资料与草稿边界说明；不接入券商接口，不触发交易 | 券商接口未配置证据、交易链路关闭证据、复核人确认无自动交易 |

## 小样本护栏

- 未获后续审批前不设可执行样本量；本草案仅定义未来候选边界。
- 所有未来候选必须保持 `data_write_flag=false`、`external_call_flag=false`、`formal_trigger_flag=false`。
- 任一禁止项命中，本草案应回滚为“草案废弃”。
- 任一后续使用前必须有人工二人复核记录。

## 验收口径

- JSON 与 Markdown 草案已创建。
- 只读验证脚本已创建，仅解析本地 JSON，不写文件、不调用外部系统。
- 未来候选仅有只读拉取、草稿生成、内部报告流转。
- 已明确仍禁止企业微信放量、n8n正式触发、正式库写入、真实媒体转换、正式税务判断、券商接口、自动交易。
- 本轮没有执行真实动作，没有导入或启用 n8n。
