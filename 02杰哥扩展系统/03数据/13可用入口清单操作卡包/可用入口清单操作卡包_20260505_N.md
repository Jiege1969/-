# 02扩展系统可用入口清单与用户操作卡包

- 生成时间：2026-05-05 15:10 +08:00
- 小任务：并行小任务N
- 状态：定义与只读验证，不执行真实动作
- 本职工作系统：暂停，不读取、不写入、不验证、不触碰
- 真实动作数量：0

## 全局边界

本包只说明 02 扩展系统当前可用入口、可用程度、用户怎么用、dry_run/真实状态、回滚方式和禁止动作。所有入口默认 `dry_run=true` 或只读/草稿模式；任何真实发送、正式库写入、真实媒体转换、券商接口、自动交易、税务正式判断或 n8n 触发均未授权。

## 总览

| 入口 | 状态 | 可用程度 | dry_run/只读 | 真实动作 |
| --- | --- | --- | --- | --- |
| 股票分析 | available_dry_run | 可做本地样例或人工输入分析草稿 | dry_run 默认开启 | 券商接口、下单、自动交易关闭 |
| 企业微信助手 | available_dry_run | 可生成回复草稿和发送前检查单 | dry_run 默认开启 | 真实发送、response_url、webhook 关闭 |
| 知识库只读问答 | available_readonly | 可做带引用的只读问答草稿 | 只读和 dry_run 默认开启 | 正式知识库、向量库、外部检索关闭 |
| 内容办公草稿 | draft_only | 可生成办公草稿、摘要、改写建议 | 草稿模式默认开启 | 发布、正式库、真实文档转换关闭 |
| 视频制作预案 | draft_only | 可生成脚本、分镜、素材清单、参数预案 | 草稿模式默认开启 | 转码、剪辑、渲染、发布关闭 |
| 税收只读证据链 | available_readonly | 可整理证据链、字段核验、缺口提示 | 只读和 dry_run 默认开启 | 税务判断、申报、税务 API、正式库关闭 |

## 操作卡：股票分析

- 入口路径：`D:\杰哥智能化系统\02杰哥扩展系统\01股票研究系统`
- 使用方式：选择人工录入或本地脱敏样例，生成股票观察清单、分析假设、风险提示和证据引用。
- dry_run 状态：开启，默认开启。
- 真实状态：关闭；不接券商、不读取账户、不下单、不自动交易。
- 回滚：作废分析草稿编号，保留人工输入和证据引用，确认券商接口和自动交易仍为关闭。
- 禁止动作：券商接口、账户资产读取、委托下单、自动交易、写交易执行队列、把草稿当正式投资建议。

## 操作卡：企业微信助手

- 入口路径：`D:\杰哥智能化系统\02杰哥扩展系统\06企业微信助手系统`
- 使用方式：使用脱敏会话样例或人工输入问题，生成回复草稿、脱敏预览和发送前检查单。
- dry_run 状态：开启，默认开启。
- 真实状态：关闭；不真实发送、不调用 response_url、不调用群机器人 webhook、不写正式消息队列。
- 回滚：作废草稿批次，保持发送配置禁用，确认 response_url 和 webhook 未调用。
- 禁止动作：企业微信真实发送、response_url、群机器人 webhook、正式消息队列写入、真实凭据读写。

## 操作卡：知识库只读问答

- 入口路径：`D:\杰哥智能化系统\02杰哥扩展系统\07知识库可追溯问答框`
- 使用方式：选择只读资料目录或样例索引，提出问题并生成带 citation 的回答草稿。
- dry_run 状态：开启，默认开启；只读来源。
- 真实状态：关闭；不写正式知识库、不写向量库、不调用外部检索 API。
- 回滚：废弃问答草稿编号，保留只读来源引用，缺证问题回到人工补证队列。
- 禁止动作：正式知识库写入、向量库 upsert、外部检索、外部抓取、覆盖原文档。

## 操作卡：内容办公草稿

- 入口路径：`D:\杰哥智能化系统\02杰哥扩展系统\04内容处理系统`
- 使用方式：选择人工输入或脱敏样本文档，生成办公草稿、摘要、改写建议和差异预览。
- dry_run 状态：开启，默认开启；仅草稿。
- 真实状态：关闭；不发布、不定时发布、不写正式内容库、不转换真实文件成品。
- 回滚：作废草稿批次号，保留源文件只读引用，不删除、不移动、不覆盖用户源文件。
- 禁止动作：正式内容库写入、发布、定时发布、真实文档转换、覆盖源文件、外部分发。

## 操作卡：视频制作预案

- 入口路径：`D:\杰哥智能化系统\02杰哥扩展系统\02视频制作系统`
- 使用方式：输入主题、素材清单或脱敏样例引用，生成脚本、分镜、素材清单和制作参数预案。
- dry_run 状态：开启，默认开启；仅预案。
- 真实状态：关闭；不转码、不剪辑、不渲染、不发布。
- 回滚：作废预案任务号，保持转码、渲染、发布队列关闭，保留素材清单引用。
- 禁止动作：真实媒体转换、转码、剪辑、成片渲染、字幕封面成品生成、发布、外部渲染服务。

## 操作卡：税收只读证据链

- 入口路径：`D:\杰哥智能化系统\02杰哥扩展系统\05税收业务系统`
- 使用方式：选择脱敏样例或只读资料目录，生成证据链清单、字段核验和缺口提示。
- dry_run 状态：开启，默认开启；只读来源。
- 真实状态：关闭；不形成正式税务判断、不出申报建议、不自动申报、不调用税务 API。
- 回滚：作废只读复核清单编号，保留证据引用和非税务意见声明，不改写正式税务资料或凭证。
- 禁止动作：正式税务判断、申报建议、自动申报、正式税务库写入、税务 API、对外税务意见。

## 必填回执字段

`package_id`、`entry_id`、`entry_name`、`requested_operation`、`use_mode`、`dry_run_flag`、`real_action_flag`、`operator`、`reviewer`、`evidence_ref`、`rollback_ref`、`blocked_actions`、`result_ref`、`timestamp_utc8`。

## 严禁触发

n8n workflow、企业微信真实发送、企业微信 response_url、群机器人 webhook、正式库、正式知识库、向量库、真实媒体转换、券商接口、委托下单、自动交易、税务 API、自动申报、删除或移动用户文件、读取或写入真实凭据。

## 只读验证

```powershell
python "D:\杰哥智能化系统\02杰哥扩展系统\02脚本\验证可用入口清单操作卡包.py"
```
