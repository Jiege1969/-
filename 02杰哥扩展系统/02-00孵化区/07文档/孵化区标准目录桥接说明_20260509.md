# 孵化区标准目录桥接说明

孵化区的核心职责是影子试验、候选能力培育、成熟度评分、人工复核、失败归档和进化候选输出。

后续孵化区仍可保留既有平铺资产，但新增能力应逐步进入标准目录：

- 配置进入 `01配置`。
- 检查器进入 `02脚本`。
- 检查结果进入 `03数据`。
- 运行日志进入 `04日志`。
- 说明和契约进入 `07文档`。

这使孵化区也具备自身层级的感知、边界、修复、记忆和进化能力。
