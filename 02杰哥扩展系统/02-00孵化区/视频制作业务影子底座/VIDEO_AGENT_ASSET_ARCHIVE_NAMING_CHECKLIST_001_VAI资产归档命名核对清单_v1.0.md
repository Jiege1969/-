# VIDEO_AGENT_ASSET_ARCHIVE_NAMING_CHECKLIST_001_VAI资产归档命名核对清单_v1.0

> 资产身份：视频创作智能体影子资产归档命名核对清单
> 阶段：W1/R0
> 状态：命名核对清单，不移动旧资产

## 命名规则

| 项目 | 规则 |
|:---|:---|
| 前缀 | 使用 `VIDEO_AGENT_` |
| 编号 | 使用三位序列或 VAI 范围 |
| 中文说明 | 保留业务含义，避免只写抽象代号 |
| 版本 | 草案和模板使用 `v1.0` |
| 日期 | 自检、回传、总收口使用 `20260508` |
| JSON配套 | 关键资产优先配套 `.json` |

## 核对项

| 核对项 | 通过条件 |
|:---|:---|
| 是否在视频制作业务影子底座目录内 | 是 |
| 是否带有 VIDEO_AGENT 前缀 | 是 |
| 是否区分模板、样例、清单、自检、回传、总收口 | 是 |
| 是否避免正式系统、上线、发布等误导词 | 是 |
| 是否不移动、不删除旧资产 | 是 |
| 是否配套 JSON 可解析 | 是 |

## 处理原则

1. 发现命名不一致时，先登记缺口，不移动历史文件。
2. 需要改名时另行建立迁移计划，不直接重命名。
3. 索引中保留旧文件引用，避免断链。
