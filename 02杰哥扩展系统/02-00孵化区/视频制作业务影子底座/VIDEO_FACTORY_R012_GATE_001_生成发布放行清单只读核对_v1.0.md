# VIDEO_FACTORY_R012_GATE_001：生成/发布放行清单只读核对

> 版本：v1.0  
> 更新时间：2026-05-08 00:05:18 +08:00  
> 资产身份：视频工厂轮次012生成/发布放行清单只读核对记录  
> 风险等级：W1  
> 边界：只读核对正式视频系统放行清单，不修改放行状态，不写回正式任务单，不触发真实渲染、上传、发布、企业微信真实外发或 n8n。  

## 一、核对对象

| 对象 | 路径 | 当前观察 |
|:---|:---|:---|
| 最新视频工厂任务单 | `D:\杰哥智能化系统\02杰哥扩展系统\02视频制作系统\03数据\18轮次012视频工厂总控层\视频工厂任务单_最新.json` | 最新任务为 `VF-20260508-003`，状态为 `预演中` |
| 视频生成放行清单 | `D:\杰哥智能化系统\02杰哥扩展系统\02视频制作系统\03数据\18轮次012视频工厂总控层\放行清单\视频生成放行清单_最新.md` | 当前登记任务为 `VF-20260508-001`，放行状态为 `待确认` |
| 视频发布放行清单 | `D:\杰哥智能化系统\02杰哥扩展系统\02视频制作系统\03数据\18轮次012视频工厂总控层\放行清单\视频发布放行清单_最新.md` | 当前登记任务为 `VF-20260508-001`，生成状态为 `待生成`，发布放行状态为 `待确认` |

## 二、关键结论

```text
最新任务 VF-20260508-003 尚未出现在生成放行清单和发布放行清单中。
```

这意味着：

1. `VF-20260508-003` 目前只能视为本地预演任务。
2. 不能因为它有任务单、脚本草案或分镜草案，就视为生成放行。
3. 不能因为存在发布放行清单文件，就视为发布放行。
4. 即使某任务未来进入生成放行清单，也仍不等于发布放行。

## 三、状态映射

| 状态来源 | 当前值 | 可推导 | 不可推导 |
|:---|:---|:---|:---|
| 任务单状态 | 预演中 | 可以继续脚本/分镜预演和人工复核 | 不能推导生成放行 |
| 生成控制.允许真实渲染 | false | 真实渲染未放行 | 不能执行渲染 |
| 生成控制.允许自动发布 | false | 自动发布未放行 | 不能执行发布 |
| 生成放行清单 | `VF-20260508-001` 待确认 | 该清单当前未放行最新任务 | 不能覆盖到 `VF-20260508-003` |
| 发布放行清单 | `VF-20260508-001` 待确认 | 发布未放行 | 不能上传、发布或外发 |

## 四、生成放行与发布放行差异

| 门禁 | 允许的动作 | 不允许的动作 |
|:---|:---|:---|
| 预演中 | 脚本草案、分镜草案、风险提示、复核问题 | 渲染、上传、发布、外发 |
| 生成放行 | 可进入真实渲染或生成预览，仍需按视频系统自身规则执行 | 不代表发布、上传、外发 |
| 已生成待预览 | 可人工预览成品 | 不代表发布 |
| 发布待确认 | 可做发布前检查 | 不代表发布 |
| 发布放行 | 仅在人工明确放行后进入发布辅助流程 | 不代表自动刷量、批量骚扰、规避平台限制 |

## 五、建议给视频系统的后续处理

以下动作应由视频系统自身完成，我方不写回：

1. 若要让 `VF-20260508-003` 进入生成流程，应由人工在生成放行清单中单独登记。
2. 若生成后要进入发布流程，应先人工预览成品，再在发布放行清单中单独登记。
3. 生成放行和发布放行应保留不同字段、不同人工确认点、不同时间戳。
4. 放行清单应避免用“最新任务”自动继承上一条任务的放行状态。

## 六、当前禁止判断

- 不判断 `VF-20260508-003` 已生成放行。
- 不判断 `VF-20260508-003` 已发布放行。
- 不判断脚本草案或分镜草案已可直接渲染。
- 不判断平台合规、版权安全或可发布。
- 不修改正式放行清单。

## 七、当前结论

```text
VF-20260508-003 当前仍为预演任务。生成放行清单和发布放行清单均未放行该任务；后续若要生成或发布，必须由视频系统和人工复核流程单独登记，不得自动继承。
```
