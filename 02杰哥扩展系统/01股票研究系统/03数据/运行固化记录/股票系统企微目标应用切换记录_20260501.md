# 股票系统企微目标应用切换记录

- 日期：2026-05-01
- 用户要求：股票分析消息不要再推送到企业微信应用 `n8n指令通行证`，目标改为 `杰哥助手`。

## 已完成

1. 新增企业微信目标应用档案：
   `D:\杰哥智能化系统\02杰哥扩展系统\00公共组件\01配置\企业微信应用目标档案.json`

2. 改造公共受控发送器：
   `D:\杰哥智能化系统\02杰哥扩展系统\00公共组件\02脚本\企业微信受控发送器.py`

   已支持参数：
   `--app-profile n8n指令通行证`
   `--app-profile 杰哥助手`

3. 更新受控发送配置生成器：
   `D:\杰哥智能化系统\02杰哥扩展系统\00公共组件\02脚本\生成企业微信受控发送配置.py`

4. 新增切换说明脚本：
   `D:\杰哥智能化系统\02杰哥扩展系统\01股票研究系统\02脚本\生成股票系统企微目标应用切换说明.py`

5. 生成切换说明报告：
   `D:\杰哥智能化系统\02杰哥扩展系统\01股票研究系统\03数据\164企微目标应用切换\股票系统企微目标应用切换说明_最新.md`

6. 生成桌面入口：
   `C:\Users\Administrator\Desktop\股票系统企微目标应用切换说明_打开.bat`

## 当前检查结果

- `n8n指令通行证`：凭据完整，可作为发送目标。
- `杰哥助手`：企业ID已配置，但 AgentId 和 Secret 尚未配置，暂不能作为真实发送目标。

## 后续切换条件

用户需在企业微信后台进入 `应用管理 -> 自建应用 -> 杰哥助手`，取得该应用的 AgentId 和 Secret，并只在本机环境文件中填写：

`D:\杰哥智能化系统\02杰哥扩展系统\00公共组件\01配置\企业微信本机环境.env`

```env
WECOM_JIEGE_ASSISTANT_AGENT_ID=这里填写杰哥助手AgentId
WECOM_JIEGE_ASSISTANT_APP_SECRET=这里填写杰哥助手Secret
```

不要把 Secret 发到聊天里。

## 安全边界

- 本次未真实发送企业微信。
- 本次未调用企业微信 API。
- 本次未触发 n8n。
- 本次未调用券商接口。
- 本次未自动交易。
- 本次未输出任何明文密钥。

## 2026-05-01 桌面 `企业微信.txt` 识别结果

用户提供桌面文件：`C:\Users\Administrator\Desktop\企业微信.txt`。

识别结论：

- 该文件包含 Bot ID、URL、Token、EncodingAESKey 一类机器人/回调参数。
- 该文件不包含 `杰哥助手` 主动推送所需的 AgentId 和 App Secret。
- Bot ID 和 Token 与本机环境文件中的机器人回调配置一致。
- 文件中的 EncodingAESKey 长度不是企业微信回调常见的 43 位格式；本机环境中现有 `WECOM_BOT_CALLBACK_AES_KEY` 为 43 位。当前企业微信接入口径统一为公网 URL 回调，不再以旧通道回环作为可用性依据，因此未覆盖本机环境配置。

处理原则：

- 不把桌面文件里的密钥内容输出到聊天或报告中。
- 不用该文件覆盖现有可用回调配置。
- 该文件可以作为 `杰哥助手` 回调/机器人配置参考，但不能替代主动应用消息推送所需的 AgentId/Secret。
