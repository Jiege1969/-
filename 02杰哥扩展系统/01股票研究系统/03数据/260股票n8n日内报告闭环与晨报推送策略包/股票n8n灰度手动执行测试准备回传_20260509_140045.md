# 股票n8n灰度手动执行测试准备回传

- 生成时间：2026-05-09 14:00:45
- 施工范围：D:\杰哥智能化系统\02杰哥扩展系统\01股票研究系统
- 本次性质：仅做本地准备、门禁整理和静态验收；不执行测试，不触发n8n，不产生新的n8n执行记录。
- 门禁结论：准备材料已归集；后续灰度手动执行测试仍为 blocked，必须另行获得人工授权后才可进行一次性手动测试。

## 已复用材料

- 139n8n主动研究编排草案：提供股票主动研究闭环编排来源。
- 142n8n未激活导入工件：目标工作流为“股票主动研究闭环_文件桥接未激活”，active=false。
- 38n8n导入前只读审计：导入前最终核验材料齐全。
- 65n8n未激活导入人工确认回执模板：仅作为确认模板，尚不代表已授权测试。
- 66/67/68/69：目标实例选择、隔离、启动前只读核验、未激活导入件。
- 247-252：主动推送灰度准备、白名单、dry-run样本、人工确认模板、交易化表达拦截、最终只读总闸口。
- 253主动推送灰度发送日志台账与回滚演练包：回滚演练通过且不发送。
- 254主动推送人工确认回执填写校验与闸口复跑包：人工确认仍未生效。
- 255主动推送单条灰度人工触发执行预案与停止开关包：单条触发预案当前拒绝执行。
- 256主动推送灰度链路总索引与操作卡：灰度链路索引完整。
- 257主动推送灰度链路一键只读巡检包：12/12 pass，真实发送仍 blocked。
- 258/259/260：股票n8n长期观察、时段调度、日内报告闭环与晨报推送策略。

## 当前核验证据

- 导入后本地核验：D:\杰哥智能化系统\02杰哥扩展系统\01股票研究系统\04日志\n8n未激活导入\stock-n8n-inactive-import-post-verify-最新.json
- 统计：pass=15，fail=0。
- 工作流名称：股票主动研究闭环_文件桥接未激活。
- 工作流ID：ZkzsB6MdxlC3VMxY。
- active：false。
- 节点类型：manualTrigger、executeCommand、set、stickyNote。
- 无webhook节点、无HTTP请求节点、无credentials字段、无企业微信真实发送节点、无券商/交易节点。
- 本轮施工未执行n8n execute/manual/webhook触发命令，未启用active=true，未发送企业微信，未接券商，不交易。

## 测试前硬门禁

- n8n目标工作流必须保持 active=false。
- 只能在另行授权后，由人工在n8n界面或受控入口手动执行一次；不得启用自动定时、webhook、cron或active=true。
- 不得真实发送企业微信；不得调用企业微信API；不得产生企业微信msgid。
- 不得群发；目标必须限定为本人单人白名单，且仍需人工确认回执完整。
- 不得接券商接口，不得下单，不得交易，不得出现买入、卖出、仓位、满仓、清仓等交易化执行指令。
- 不得重载19310/19302。
- 不得修改总管面板，不得修改一键接续包。
- 如250/252/254/255仍为blocked，则不得进入真实发送链路。

## 后续测试输入定义

- 目标工作流：股票主动研究闭环_文件桥接未激活。
- 触发方式：仅人工一次性手动触发；本次准备阶段不触发。
- 工作流内置触发文件：/home/node/.n8n/jiege_bridge/stock_active_research_trigger.json。
- 触发文件预期字段：task=stock_active_research；mode=inactive_intraday_loop；stage=manual_test_or_schedule；created_at=执行时生成。
- 测试解释约束：即使stage字段含schedule字样，灰度手动测试也只能按manual one-shot解释；如无法证明未启用自动定时，则阻断。
- 外发输入：无；不得携带企业微信真实发送参数、群发目标、券商凭据或交易参数。

## 后续测试预期产物

- 本次准备产物：仅本地Markdown/JSON回传，不产生n8n执行记录。
- 后续获授权测试产物：最多1条n8n手动执行记录；1个触发文件；1份本地测试回传；0条企业微信真实发送；0个企业微信msgid；0个群发目标；0个券商/交易动作。
- 若执行后发现多于1条执行记录、出现自动定时、出现外发、出现券商/交易字段，立即判定失败并阻断后续。

## 停止开关

- 人工未授权或撤回授权：立即停止，不进入n8n手动测试。
- active不是false：立即停止，先恢复未激活状态。
- 发现webhook、cron、schedule自动触发或active=true：立即停止，不执行手动测试。
- 企业微信发送闸口未放行：不调用企业微信API，不生成msgid。
- 目标不是本人单人白名单：阻断群发风险。
- 命中交易化表达、券商接口、交易动作：登记高风险事件并停止。
- 出现重复执行、多次点击、自动重试：停止并只保留失败回传。

## 回滚方式

- 默认回滚：保持工作流active=false，保留当前未激活导入状态，不做active=true。
- 如后续测试只写入触发文件但未被消费：隔离或清理触发文件，记录为未发送，无需撤回消息。
- 如后续测试产生失败执行记录：不重跑，不补发，记录失败阶段，转人工复核。
- 如误触发外发风险：立即停用相关发送入口，保留证据，按253回滚演练包登记，不做二次尝试。
- 如出现服务级风险：不得重载19310/19302，由人工另行授权处理。

## 失败阻断项

- active=false无法确认。
- 手动测试授权缺失。
- 不是仅手动一次，或存在自动定时/自动重试。
- 出现企业微信真实发送、msgid、群发目标或发送API调用。
- 出现券商连接、交易参数、下单动作或交易化执行指令。
- 250人工确认模板未完成、252最终总闸口blocked、254回执校验blocked、255单条触发预案拒绝执行。
- 导入后核验不再保持pass=15/fail=0，或工作流节点出现webhook/HTTP/credentials/发送节点。

## 本次本地验收

- 已完成：静态查找现有n8n本地手动测试、灰度、停止开关、回滚演练材料。
- 已完成：复用既有139、142、38、65、66、67、68、69、247-260材料。
- 已完成：生成灰度手动执行测试准备回传。
- 未执行：n8n execute、n8n手动触发、webhook触发、active=true、企业微信真实发送、券商连接、交易、19310/19302重载、总管面板/一键接续包修改。

## 结论

《股票n8n灰度手动执行测试准备》已完成本地材料归集和门禁固化；当前仅允许进入“等待人工授权”状态，不允许直接执行测试。
