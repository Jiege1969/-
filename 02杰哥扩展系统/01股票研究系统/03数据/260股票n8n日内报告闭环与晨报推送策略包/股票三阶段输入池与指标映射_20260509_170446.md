# 股票三阶段输入池与指标映射

- 生成时间：2026-05-09 17:04:47
- 定位：三阶段报告输入池与技术指标映射，不是交易规则，不发送企业微信。

## 一、股票池分层

| 层级 | 数量 | 角色 | 样例 |
| --- | ---: | --- | --- |
| 用户重点关注池 | 123 | 用户偏好和长期关注入口 | 浙商中拓(sz000906)、正丹股份(sz300641)、新易盛(sz300502)、中钨高新(sz000657)、回盛生物(sz300871) |
| 300只试运行池 | 300 | 大样本试运行基础池 | 浙商中拓(sz000906)、正丹股份(sz300641)、新易盛(sz300502)、中钨高新(sz000657)、回盛生物(sz300871) |
| 300只盘后轻扫描 | 10 | 盘后短线初筛候选 | 海光信息(sh688041)、光迅科技(sz002281)、豪威集团(sh603501)、兆易创新(sh603986)、药明康德(sh603259) |
| L8X综合候选池 | 824 | 大样本综合候选底座 | 浦发银行(sh600000)、白云机场(sh600004)、中国国贸(sh600007)、首创环保(sh600008)、上海机场(sh600009) |
| L7可交易过滤池 | 823 | 过滤ST、低质量和不可交易样本后的候选池 | 浦发银行(sh600000)、白云机场(sh600004)、中国国贸(sh600007)、首创环保(sh600008)、上海机场(sh600009) |
| L6行业主题观察池 | 80 | 行业主题和长线方向入口 | 龙芯中科(sh688047)、中微公司(sh688012)、摩尔线程(sh688795)、中芯国际(sh688981)、海光信息(sh688041) |
| L5深度研究池 | 10 | 晚间深度研究候选 | 龙芯中科(sh688047)、中微公司(sh688012)、摩尔线程(sh688795)、华虹公司(sh688347)、天齐锂业(sz002466) |

## 二、技术指标分层

| 指标产物 | 数量 | 已有指标 | 缺口指标 |
| --- | ---: | --- | --- |
| 重点关注池技术指标 | 19 | MA5/10/20/60、RSI14、MACD、成交量MA20、量比5日 | KDJ、BOLL、ATR、换手率、振幅、缺口、支撑位、压力位、平台突破、回踩确认、竞价强弱 |
| 300只候选技术指标 | 10 | MA5/10/20/60、RSI14、MACD、成交量MA20、量比5日 | KDJ、BOLL、ATR、换手率、振幅、缺口、支撑位、压力位、平台突破、回踩确认、竞价强弱 |

## 三、三阶段输入映射

### 盘后短线复盘
- 标题：【收盘短线观察｜YYYY-MM-DD】
- 主输入池：300只试运行池、300只盘后轻扫描
- 辅助输入池：用户重点关注池、L7可交易过滤池
- 当前可用字段：现价、涨跌幅、成交额、流通市值、轻扫描评分、候选理由、风险和降级说明、MA、RSI14、MACD、成交量MA20、量比5日
- 当前不足：300只盘后轻扫描只有10只候选输出、技术指标当前只覆盖重点关注19只和候选10只、缺少支撑位/压力位/止损线的标准计算字段
- 可先落地方式：先用300只盘后轻扫描候选清单生成短线观察，再用已有技术指标补充能匹配到的股票；无法匹配的股票标为指标不足，不进入高优先级。

### 晚间三维深度分析
- 标题：【深度三维研究｜YYYY-MM-DD】
- 主输入池：L6行业主题观察池、L5深度研究池
- 辅助输入池：用户重点关注池、300只盘后轻扫描
- 当前可用字段：行业、细分领域、行业强度、资金活跃度、用户增强优先席位、技术结构、财报候选证据
- 当前不足：行业指数周线趋势未统一入字段、政策催化未结构化接入、订单/产能/机构关注字段未统一
- 可先落地方式：先输出行业主题、成长证据缺口和短线共振三段，不把缺口写成推卸责任，而是写成影响优先级的客观限制。

### 盘前出击排序
- 标题：【盘前出击排序｜YYYY-MM-DD】
- 主输入池：前一晚深度三维研究、盘后短线复盘候选
- 辅助输入池：盘前消息字段、竞价字段
- 当前可用字段：昨晚候选、触发价、止损/放弃线、行业方向
- 当前不足：盘前突发消息、外盘、夜盘期货、集合竞价情绪尚未结构化接入
- 可先落地方式：在盘前外部字段未接入前，只输出基于昨晚条件的预排序，并明确盘前消息字段为空，不编造消息。

## 四、指标到报告语言映射

- MA5/MA10/MA20/MA60：判断短线位置、趋势层级、承接区和强弱线。
- MACD：判断趋势动能是否配合，只能辅助，不单独构成关注条件。
- RSI14：判断是否过热或弱势，不直接写成买卖理由。
- 成交量MA20/量比5日：判断放量、缩量和资金确认度，是盘后短线复盘的核心字段。
- 现价/涨跌幅/成交额/流通市值：判断盘后轻扫描入池、市场活跃度和样本代表性。
- 缺失字段处理：没有支撑压力、BOLL、ATR、换手率、竞价字段时，不允许写强确定性结论，只能给条件式观察。

## 五、第一轮改进结论

- 先不扩2000只，先把300只试运行池和92盘后轻扫描接入三阶段报告。
- 先不追求全部指标，先把已有MA/RSI/MACD/量比翻译成触发条件和止损线。
- 盘前报告暂不编造外盘、夜盘、突发消息和竞价情绪；这些字段未接入前，只能作为待接入项。
- 下一步应优先生成盘后短线复盘正式样例，输入必须来自300只盘后轻扫描和现有技术指标，而不是手写3只样本。
