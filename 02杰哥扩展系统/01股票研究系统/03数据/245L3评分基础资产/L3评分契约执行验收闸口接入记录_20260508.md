# L3评分契约执行验收闸口接入记录

日期：2026-05-08

范围：股票分析系统L3评分报告与企业微信短答链路。

目的：防止后台评分报告继续残留旧市场风格口径，确保分数、分项、证据、缺口、置信度和结论阈值可自动验收。

## 本次接入

- 生成规则：`02脚本/生成单股L3评分报告.py` 的 `missing_summary` 改为读取各分项真实 `missing` 字段；市场风格为 `confirmed` 时不再固定写入旧的 `partial` 补全口径。
- 验收闸口：新增 `02脚本/验证L3评分契约执行.py`，检查总分范围、分项权重、分项合计、结论阈值、置信度与旧市场风格口径残留。
- 统一验收：`02脚本/运行L3短答证据刷新与验收.py` 接入 `l3_scoring_contract_validation` 步骤。
- 样本清理：机械清理 `03数据/245L3评分基础资产/单股L3评分/*.json` 中旧市场风格话术，不改分数、不改结论、不新增强判断。

## 边界

- 不真实发送企业微信
- 不触发 n8n
- 不调用券商接口
- 不自动交易
