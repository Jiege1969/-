# 智能体影子层下一轮候选队列

- 生成时间：2026-05-08 09:59:18
- 真实系统触发：否

## 内容

### 队列定位
- 用途：
  - 为连续自动施工提供本业务线内部候选来源，减少等待确认
- 适用范围：
  - 视频制作影子工厂、人工复核链、模板、清单、只读检查器
- 真实系统触发：
  - False

### 下一轮候选
-
  - 优先级：
    - P0
  - 候选任务：
    - 成品预览回执样例
  - 可做动作：
    - 定义预览通过、需要修改、驳回三类回执字段
  - 红线：
    - 不打开真实成品、不发送企业微信、不调用发布工具
-
  - 优先级：
    - P0
  - 候选任务：
    - AI标识人工确认回执
  - 可做动作：
    - 定义发布前 AI 标识确认字段和缺口问题
  - 红线：
    - 不替用户勾选平台选项、不连接真实账号
-
  - 优先级：
    - P1
  - 候选任务：
    - 平台规则字段待补清单
  - 可做动作：
    - 列出小红书、抖音、视频号、B站差异化待补字段
  - 红线：
    - 不抓取平台页面、不声明为最新正式规则
-
  - 优先级：
    - P1
  - 候选任务：
    - 任务回溯索引样例
  - 可做动作：
    - 定义从想法、任务单、草案、复核、放行到结果的本地索引字段
  - 红线：
    - 不写正式知识库、不改 R010/R011 旧资产
-
  - 优先级：
    - P2
  - 候选任务：
    - 企业微信命令缺口卡
  - 可做动作：
    - 列出杰哥视频助理未来接入前缺少的字段和权限问题
  - 红线：
    - 不修改公共企业微信配置、不真实发送

### 自动续建规则
- 每轮开始先只读总管施工面板和一键接续包。
- 优先选择 P0 且不触发红线的候选任务。
- 若候选任务缺少输入，就自动转为缺口卡、模板、字段契约或只读检查报告。
- 每轮结束必须在本业务线目录生成对齐回传记录。

### 停止条件
- 需要修改总管面板或一键接续包。
- 需要修改企业微信公共接入配置、n8n、服务脚本或正式入口。
- 需要触发真实渲染、真实发布、真实发送、真实账号或平台操作。
- 缺少关键上下文且无法转为低风险占位产物。

## 边界声明

- 本文件是轮次012视频创作智能体影子层设计，不是正式上线能力。
- 不调用真实大模型，只预留未来适配器契约。
- 不发送企业微信，只保留未来输入输出终端口径。
- 不读取真实素材、不访问平台、不连接真实账号。
- 不接 n8n。
- 不真实渲染、不真实发布。
- 生成放行、真实渲染放行、发布放行必须拆开。
- “发吧”“生成”“发布”等命令只能解析为待复核意图，不能直接执行。
