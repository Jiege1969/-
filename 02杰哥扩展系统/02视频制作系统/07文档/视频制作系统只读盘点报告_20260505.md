# 视频制作系统只读盘点报告

生成日期：2026-05-05

盘点范围：`D:\杰哥智能化系统\02杰哥扩展系统\02视频制作系统`

本轮约束：只读盘点为主；未运行视频制作 Python 业务脚本；未复制、移动、转码或生成真实媒体；未调用剪辑软件；未发送企业微信；未触发 n8n；未触及股票自动交易链路。

## 一、目录与资产概况

盘点时目录内共有 166 个文件，合计约 308627 字节。文件类型分布为：JSON 106 个、Markdown 49 个、Python 8 个、Python 缓存 3 个。未发现 `.mp4`、`.mov`、`.avi`、`.mkv`、`.mp3`、`.wav`、`.aac`、`.jpg`、`.jpeg`、`.png`、`.webp` 等真实媒体文件。

顶层目录如下：

| 目录 | 当前用途 | 观察 |
| --- | --- | --- |
| `01配置` | 视频制作配置、模板、桥接规则、门禁开关 | 配置完整，安全边界明确 |
| `02脚本` | 本地 JSON 生成与验收脚本 | 8 个 Python 脚本，均为标准库为主 |
| `03数据` | 脚本草稿、分镜、计划、素材候选、主题预演、门禁、状态摘要 | 以“最新”文件和历史快照为主 |
| `04日志` | 状态摘要验证日志 | 最近验证日志显示 11 通过、0 失败 |
| `05备份` | 预留 | 本轮未展开写入 |
| `06临时` | 预留 | 本轮未展开写入 |
| `07文档` | 系统设计、阶段说明、门禁登记和本轮报告 | 本轮只在此目录新增交付文档 |

## 二、配置盘点

| 配置文件 | 能力含义 | 安全边界 |
| --- | --- | --- |
| `视频制作配置.json` | 第一阶段任务计划、脚本草稿、分镜框架配置 | 不剪辑真实视频、不转写真实音频、不上传外部平台、不调用付费接口 |
| `视频素材登记模板.json` | 素材登记模板 | 当前未看到已填充素材登记数据 |
| `视频脚本模板.json` | 口播脚本结构与默认风格 | 要求人工复核事实和发布风险 |
| `视频分镜模板.json` | 默认镜头字段和分镜模板 | 不驱动剪辑软件 |
| `视频素材桥接规则.json` | 读取内容处理系统素材索引，生成候选和匹配预演 | 允许读取索引；禁止复制、移动、删除、转码、调用剪辑软件、发布视频 |
| `视频素材本地整理门禁.json` | 本地整理门禁开关 | 删除、移动、覆盖、真实渲染、自动发布、n8n、企业微信真实发送均为 false |
| `视频主题化生成规则.json` | 主题方案、字幕要点、封面标题预演规则 | 不调用剪辑软件、不生成真实媒体、不上传发布、不使用未授权素材 |

## 三、脚本盘点

| 脚本 | 主要产物 | 读写范围与风险 |
| --- | --- | --- |
| `生成视频制作计划.py` | `03数据/04任务计划`、`03数据/02脚本草稿`、`03数据/03分镜计划` | 本地生成 JSON；不处理媒体 |
| `生成视频素材候选.py` | `03数据/05素材候选` | 读取 `04内容处理系统` 素材索引；若索引缺失会尝试运行内容索引生成脚本，属于下一步需收紧的隐性触发点 |
| `生成视频分镜素材匹配.py` | `03数据/06分镜素材匹配` | 若候选或分镜缺失会调用本模块生成脚本；只做候选推荐 |
| `生成视频主题化预演.py` | `03数据/07主题化预演` | 若脚本草稿或分镜缺失会调用计划生成脚本；只生成文本预演 |
| `生成视频素材本地整理门禁报告.py` | `03数据/08本地整理门禁` | 会串行运行候选、匹配、主题预演脚本；本轮未执行 |
| `执行视频真实渲染禁用态检查.py` | `03数据/08本地整理门禁` | 明确输出真实渲染禁用态；不调用剪辑软件 |
| `生成视频制作系统状态摘要.py` | `03数据/09状态摘要` | 汇总状态和安全边界；安全边界含 `接入交易: false` |
| `验证视频制作系统状态摘要.py` | `04日志` | 会运行状态摘要生成脚本并写日志；本轮未执行 |

脚本关键词扫描未发现 `requests`、`urllib`、HTTP URL、webhook、剪辑软件调用、ffmpeg/moviepy/Premiere/达芬奇/剪映、股票交易库或券商调用。发现的 `subprocess.run` 均为本地 Python 脚本链式调用。

## 四、现有可交付能力地图

| 能力 | 当前成熟度 | 可交付产物 | 人工依赖 |
| --- | --- | --- | --- |
| 第一阶段视频制作计划 | 可用 | 任务计划、脚本草稿、分镜计划 | 主题、受众、时长需要人工确认 |
| 内容素材候选桥接 | 可用但需门禁 | 视频素材候选 JSON | 素材来源和授权必须人工确认 |
| 分镜素材匹配预演 | 可用 | 每个镜头候选素材列表 | 匹配质量、素材授权、剪辑动作需人工确认 |
| 主题化预演 | 可用 | 主题方案、字幕要点、封面标题预演 | 事实依据、标题风险、发布语境需人工确认 |
| 本地整理门禁 | 可用 | 门禁报告、准入要求、下一步清单 | 放行真实渲染前需单独审批 |
| 真实渲染禁用态 | 可用 | 禁用态检查 JSON | 禁止自动解除 |
| 状态摘要与验证 | 可用 | 状态摘要 Markdown/JSON、验证日志 | 本轮未重新生成，沿用现有最新文件 |
| 真实视频生产 | 未开放 | 无 | 需要人工审批、素材授权、渲染放行单和发布确认 |

## 五、现状状态

最新状态摘要文件：`03数据/09状态摘要/video-production-system-status-summary-最新.json`

摘要时间：2026-05-04 07:14:42

状态：`healthy`

关键指标：素材候选数 1、分镜素材匹配数 3、主题方案数 3、字幕要点数 3、门禁失败脚本数 0、真实渲染状态 `禁用态`、是否调用剪辑软件 `false`。

安全边界：调用剪辑软件 `false`、执行真实渲染 `false`、触发 n8n `false`、发送企业微信 `false`、写旧系统 `false`、接入交易 `false`。

## 六、缺口与风险

1. `03数据/01素材登记` 当前未见已登记素材文件，素材授权链路还没有形成可验收台账。
2. 候选桥接依赖 `04内容处理系统/03数据/02登记索引/内容素材索引_最新.json`；当前索引存在，但脚本在索引缺失时会自动调用外部系统脚本，低风险施工前应增加“只读/禁止补生成”运行模式。
3. 多个脚本会在缺失依赖时链式调用本地脚本并写入 `03数据` 或 `04日志`，适合日常生成，不适合本轮这种只读验收任务。
4. `__pycache__` 存在 3 个缓存文件，对能力无影响，但后续交付清单可忽略缓存。
5. 最新状态摘要为 2026-05-04，主题化预演最新文件为 2026-05-05；本轮未重新运行脚本，因此状态摘要未刷新。
6. 真实渲染、剪辑、发布、企业微信和 n8n 均处于关闭态；这属于安全能力，不是产能缺口。不得为了“打通链路”绕过这些硬闸门。

## 七、股票自动交易屏蔽硬闸门

本系统继续继承股票自动交易屏蔽硬闸门：

- 不读取、修改或调用任何股票自动交易脚本。
- 不接入券商、行情、下单、回测或交易通知接口。
- 不把视频制作状态、素材信息、主题内容写入交易系统。
- 任意报告、状态摘要或验收项中出现 `接入交易` 时，必须保持 `false`。
- 若后续新增自动化编排，必须显式断开股票自动交易系统依赖。
